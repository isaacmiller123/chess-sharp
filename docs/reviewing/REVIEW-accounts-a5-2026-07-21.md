# A5 review — verified open issues (canonical anticheat judge)

**Status:** ADJUDICATED 2026-07-22 — all 33 confirmed findings resolved: **30 FIXED** (incl.
A5-21, decided + re-anchored on the 5σ conviction per the owner directive "honest players are
never banned") and **3 DEFERRED, ratified** (A5-14 / A5-17 / A5-36 — each deferral is the
false-positive-safe choice; see the entries for the revisit events). A5 was committed at
`d81594a` on 2026-07-22 (the A4–A6 checkpoint commit). **Review date:** 2026-07-21; fix phase
2026-07-21→22.

**Method.** Fable-pinned multi-agent Workflow, owner-specified group progression: (1) **search+verify**
— 7 agents, one per A5 dimension (judge core, Tier-1, Tier-2, anchors/estElo, bans+fold, proof/test
integrity, spec congruency), each self-verifying before returning; (2) **implication search** — 5 agents
over the full found set (same-class-elsewhere, downstream-consumers, invariant-violations, test-gaps,
cross-phase congruency); (3) **dedup** → 38 canonical findings; (4) **unbiased verifier round** — per
finding, two independent agents that did NOT find it: a technical refuter (default-refuted, reproduce
from code) AND a relevance/congruency judge who decides whether it matters for the A5 end goal (§8
bit-deterministic reproducible verdicts · §0 pure-fold-over-verified-data · Tier-2-only conviction ·
§7b unpredictable-before/recomputable-after salt) — **relevance decided here, by the verifiers, not by
the searchers**; (5) synthesis. (The run was interrupted twice by API/spend limits and resumed from
cache; the verifier round is complete for all 38 findings.)

**Funnel:** 27 search + 20 implied → 38 deduped → unbiased verify → **33 confirmed, 5 dropped.**

**Totals (confirmed, 33): 3 critical · 16 major · 14 minor.** Each below was confirmed by the technical refuter AND judged relevant/congruent to the A5 end goal by the independent relevance judge.

**When an issue is fixed**, delete its entry from this file (per `docs/DOC-GUIDE.md`). When empty, keep only the header.

---

## Summary

| ID | Sev | File | Issue |
|----|-----|------|-------|
| A5-01 | CRIT | `src/shared/accounts/judge/judge.ts:233` | Canonical transcript->positions judging surface (which plies, what encoding) is not pinned in the shared core; in-repo callers already diverge in verdict-bit-changing ways |
| A5-02 | CRIT | `src/shared/accounts/judge/tier2.ts:559` | Tier-2 z-statistic scores signals against the lagging fold display rating as `elo`, so honest strong/improving players self-convict during the rating climb; the zero-FPR proof only ever exercises elo == true strength |
| A5-03 | CRIT | `src/shared/accounts/judge/tier2.ts:988` | verifyTier2Verdict blesses a 'suppression' record whose trailing-K z never reached the escalation trigger — mints false suppression against honest accounts |
| A5-04 | MAJO | `scripts/lib/accounts-fixture.mjs:519` | Judge-adapter liveness contract (onError makes an in-flight judgeGame fail instead of hang) has zero coverage on both adapters |
| A5-06 | MAJO | `scripts/test-accounts-tier1.mjs:437` | tier1Record fail-closed matrix has no wrong-(nodes, multiPv, hashMb) case — a Tier-2-config or nodes=1 'Tier-1' record is mintable with every suite green |
| A5-07 | MAJO | `scripts/test-accounts-tier2.mjs:543` | Suppression receipts have zero negative coverage: no sub-escalation suppression case, and the header-claimed `kind` tamper assertion does not exist |
| A5-08 | MAJO | `scripts/test-accounts-tier2.mjs:641` | Fold-bans tests pin unconditional 'newest wins' with only growing expiries — the un-ban path and the deterministic-deadline validation have no test anywhere |
| A5-09 | MAJO | `scripts/test-judge-calibration.mjs:308` | Honest zero-FPR proof is cross-sectional and circular — it structurally cannot exhibit the persistent per-account anchor-residual offset the production longitudinal trigger accumulates on (holds even at elo == trueElo) |
| A5-10 | MAJO | `scripts/test-judge-fit.mjs:204` | Engine-identity guard for the J3 fit is vacuous: F.wasmSha256 does not exist, so the assertion compares PARAMS_A5.judgeWasmSha256 to itself — a judge-engine swap slips past the fit suite |
| A5-12 | MAJO | `server/judge/contentHash.ts:33` | Judge WASM hash pinned in two unlinked literals (PARAMS_A5.judgeWasmSha256 vs contentHash.ts JUDGE_WASM_SHA256) with no equality assertion anywhere |
| A5-13 | MAJO | `server/judge/nodeAdapter.ts:30` | Node content-hash gate verifies a file the engine never loads: checked-vs-loaded split plus TOCTOU (web blob-URL path is immune) |
| A5-14 | MAJO | `src/shared/accounts/judge/tier1.ts:260` | Score-equivalence window is dead for in-list moves: list membership alone certifies an engine match (rank-4 self-match), and out-of-list blunders match any line's score |
| A5-15 | MAJO | `src/shared/accounts/judge/tier1.ts:328` | Fischer-increment/byo-yomi credit is baked into the signed snapshots, so honest fast play yields clockFitMicro = 0 — the hardcoded 'machine pacing' maximal-suspicion value |
| A5-16 | MAJO | `src/shared/accounts/judge/tier1.ts:511` | Clock forensic consumes unvalidated clock inputs no honest party checks: the accused's own self-asserted clockMs (`after`) AND the opponent-signed `before` snapshot — either side can drive clockFitMicro to an extreme |
| A5-18 | MAJO | `src/shared/accounts/judge/tier2.ts:349` | Window salt is not a unique function of (root, ladder, window, witness set): any threshold subset of grants is 'valid', so the reveal publisher grinds the boundary post-hoc and honest auditors with different reveals disagree |
| A5-20 | MAJO | `src/shared/accounts/judge/tier2.ts:741` | escalationDue returns the trailing-K firing even when the J7 lifetime trigger escalated EARLIER in chain order — the self-ban deadline is set too late, minting false suppression / a consensus split |
| A5-21 | MAJO | `src/shared/accounts/judge/tier2.ts:1102` | Self-ban / suppression obligation is gated on the 3σ escalation trigger, not the 5σ conviction — ban FPR is ~1.3e-3/window, not astronomically low |
| A5-22 | MAJO | `src/shared/accounts/ratings/fold.ts:323` | Self-ban expiry is self-asserted and non-monotonic — a convicted account un-bans itself with a second selfban |
| A5-23 | MAJO | `src/web/engines/judge.ts:105` | Web adapter: close()/killed worker never notifies onError and send() silently no-ops, so an in-flight or post-close judgeGame hangs forever (node counterpart fails closed) |
| A5-11 | MINO | `scripts/test-judge-node.mjs:352` | 'Tampered wasm refused' test only proves the wasmPath ARGUMENT is hashed — the §8 invariant 'verified bytes are the executed bytes' has no test and a divergent checked/loaded pair passes every suite |
| A5-17 | MINO | `src/shared/accounts/judge/tier2.ts:296` | Window salt commits to no post-game entropy and no time binding, so the frontier is computable BEFORE the games — §7b 'unpredictable before' rests entirely on unenforced witness signing-time discipline |
| A5-24 | MINO | `scripts/operator-smoke.mjs:111` | Operator peer's 'last-resort Tier-2' judge capability is tested only for construction in a non-CI smoke script — its analyseFixedNodes output is never cross-checked against the canonical judgeGame surface |
| A5-26 | MINO | `scripts/test-accounts-tier2.mjs:614` | selfBanDueNow tests pin the ban obligation at a 3.41σ escalation and no test anywhere separates escalation consequences from the 5σ conviction line |
| A5-27 | MINO | `scripts/test-judge-calibration.mjs:146` | The 'corpus params digest == CURRENT PARAMS_A5_DIGEST' gate is satisfiable by editing a JSONL field — the stamps were already migrated in place after the J7 drift, and the one bit-level receipt is skipped under CI |
| A5-28 | MINO | `scripts/test-judge-calibration.mjs:199` | sidesOf() sort comparator is inconsistent on (idx, side) ties, and the multi-run thresh family has full ties — section-5 threshold-ε window selection and §7(a) fold order are implementation-defined, not spec-deterministic |
| A5-29 | MINO | `scripts/test-judge-calibration.mjs:530` | The only cross-artifact judging receipt re-implements the transcript->positions mapping inline (bare fenBefore, one honest row), so no test can detect a judged-surface or encoding divergence between producers |
| A5-31 | MINO | `scripts/test-judge-node.mjs:113` | Comment claims the J6 browser gate asserts the SAME frozen 9-position Tier-1 digest from the web adapter; the browser gate actually checks a 3-position subset against a live node oracle, never GOLDEN_T1_DIGEST |
| A5-32 | MINO | `server/operator/peer.ts:112` | Operator peer's 'last-resort Tier-2' judge is the non-canonical A2 analyseFixedNodes surface, whose protocol and parse rules differ bit-wise from the canonical judgeGame path |
| A5-33 | MINO | `src/shared/accounts/judge/anchors.ts:128` | Orphaned doc block says the J3 estElo anchor set 'is what may feed T', contradicting the J6 rule that only TIER2_ANCHORS_JUDGE feeds T — and nothing at runtime blocks the biased set from being injected |
| A5-34 | MINO | `src/shared/accounts/judge/anchors.ts:161` | TIER2_ANCHORS_JUDGE provenance claims honest max z 1.884σ / held-out 1.932σ / margin ≥1.07σ — the committed suite over the committed corpus deterministically measures 1.482σ / 2.123σ (held-out margin 0.877σ) |
| A5-36 | MINO | `src/shared/accounts/judge/tier1.ts:347` | The §8 strength-trajectory Tier-1 signal is computed and unit-tested but never persisted in Tier1Record nor consumed by Tier-2/T — the smurf / rapid-improvement channel the spec mandates is absent from every verdict |
| A5-37 | MINO | `src/shared/accounts/judge/tier1.ts:590` | tier1Record accepts a JudgeOutput judged at ANY (nodes, multiPv, hashMb) — only the params digest is checked, and the record drops the config, so wrong-config signals are indistinguishable without a full re-judge |
| A5-38 | MINO | `src/shared/accounts/ratings/fold.ts:147` | a4-v1 `bans` checkpoint state is unbounded in attacker-controlled ladder strings |

---

## Critical (3)

### A5-01 — [FIXED] Canonical transcript->positions judging surface (which plies, what encoding) is not pinned in the shared core; in-repo callers already diverge in verdict-bit-changing ways

- **File:** `src/shared/accounts/judge/judge.ts`  ·  **Severity:** critical  ·  **Status:** FIXED 2026-07-21

**Fix.** judge.ts now exports the NORMATIVE `transcriptToJudgePositions(moves, fenBeforeOf)` — exactly `[{ply:i, fen:fenBeforeOf(i)}]` for i in 0..moves.length−1: every transcript ply, bare fenBefore, no tail, never the `fen START moves …` path encoding — deliberately pinned to the surface TIER2_ANCHORS_JUDGE was measured on (gen-cheater-corpus recordGame), so NOTHING was re-judged. tier1Record now enforces the surface fail-closed at the trust boundary: the judged ply set must equal EXACTLY {0..moves.length−1} (contiguous, complete, none missing, none ≥ moves.length) and any judged position carrying a `moves`/path field is rejected (bare-FEN pin) — closing the cherry-picked-subset escalation evasion independent of how the producer built its positions. Verdict-path callers (gen-cheater-corpus recordGame; the test-judge-calibration §7 cross-artifact receipt) now route through the builder; bits unchanged.

**Verified.** New deterministic regressions in scripts/test-accounts-tier1.mjs §(i) (synthetic JudgeOutputs, no engine): a {0,2}-subset of a 6-ply transcript, an interior gap, a prefix subset, and a moves-path-carrying position are all REJECTED by tier1Record with Tier1InputError (the subset was ACCEPTED pre-fix with empty signals); a full-coverage record is still ACCEPTED with tier1Digest byte-identical to the pre-fix frozen value (`chzY1umBfAfE9M6Ce6u_73xtvCaMEIwOK7dw_mKVm-8`); builder agreement — two independent callers over the same (moves, fenBeforeOf) produce identical positions. Suites green: test-accounts-tier1.mjs (174), test-accounts-tier2.mjs (252, frozen goldens intact), test-judge-node.mjs (42 — its raw-protocol moves-path golden exercises the J1 protocol, not a verdict record, and is untouched), test-judge-calibration.mjs non-engine sections (55), npm run typecheck (all three tsconfigs).

**Residual.** gen-judge-corpus's fenBefore+tail surface is left in place as a documented NON-verdict residual: it feeds only the JUDGE_ELO_FIT estElo fit (tier1Record already rejects its tail via the judged-ply ≥ moves.length check) and should be pinned separately at the next estElo re-fit. FEN board-replay stays caller-side by design — `fenBeforeOf` is caller-supplied, no chess library enters the shared tree. sideMoveScores still accepts partial views for diagnostics only; they can never form a record.

### A5-02 — [FIXED] Tier-2 z-statistic scores signals against the lagging fold display rating as `elo`, so honest strong/improving players self-convict during the rating climb; the zero-FPR proof only ever exercises elo == true strength

- **File:** `src/shared/accounts/judge/tier2.ts`  ·  **Severity:** critical  ·  **Status:** FIXED 2026-07-21

**Fix.** gameDevMicro now scores against an RD-aware upper-confidence strength: an optional `rdMicro` (the a4-v1 fold's ladder RD entering the game, micro-Elo) was added to `WindowEntry`/`LadderGameRef` and threaded through escalationDue's entry producer; when present, anchor expectations evaluate at `effElo = elo + floor(RD_CONF_MUL·rdMicro/1e6)` with `RD_CONF_MUL = 2` — a LOCAL [A5-CALIBRATED] const in tier2.ts (settled RD-30 account shifts ≤ 60 Elo, placement/climbing RD-300+ shifts ≥ 600, covering the lag). `rdMicro` absent ⇒ byte-identical legacy path, so every frozen tier2 golden holds un-refrozen.

**Verified.** New deterministic regression in scripts/test-accounts-tier2.mjs (real TIER2_ANCHORS_JUDGE): honest true-2700 fresh climb (corpus-band signals 25.22cp/0.9388, sustained-lag elo/RD schedule) — rd-absent z = 5.961σ (reproduces the false escalation/conviction), rd-present z = 2.646σ (below the 3.0σ trigger); settled honest 2500 at RD floor 30 — z shifts 0 → −0.282σ (≤ 0.3σ, downward — calibrated FPR preserved); full-engine cheater (acpl 0, match 1.0) under the SAME high RD — z = 7.732σ, still convicts in one window. Suites green: test-accounts-tier2.mjs (252 asserts, all pre-existing goldens intact), test-accounts-tier1.mjs (165), npm run typecheck.

**Residual.** test-judge-calibration.mjs (engine-heavy) was not re-run; its holdout never supplies rdMicro so it is byte-unaffected, but the §14-A5 honest-holdout margin should be re-derived with rdMicro exercised at the next calibration pass, and RD_CONF_MUL folded into PARAMS_A5 at the next calibration re-pin (deliberately kept out now to avoid drifting PARAMS_A5_DIGEST).

### A5-03 — [FIXED] verifyTier2Verdict blesses a 'suppression' record whose trailing-K z never reached the escalation trigger — mints false suppression against honest accounts

- **File:** `src/shared/accounts/judge/tier2.ts`  ·  **Severity:** critical  ·  **Status:** FIXED 2026-07-22

**Fix.** verifyTier2Verdict now enforces, for kind:'suppression', that the deterministic escalation trigger actually FIRED as recomputed from its own inputs (the derivable-from-inputs contract): the window path requires a FULL reganK window (entries.length === PARAMS_A5.reganK) with recomputed zMicro ≥ PARAMS_A5.zEscalateMicro (escalationDue's exact trailing-K condition), OR the J7 lifetime path requires the record's recomputed lifetimeVerdict(windowZs).zLifeMicro ≥ zEscalateMicro (reusing the existing lifetime recompute, no duplicated math); neither ⇒ rejected with the deterministic sorted error 'verdict: suppression trigger did not fire on the recomputed evidence (…)'. makeTier2Verdict throws symmetrically (explicit early Tier2InputError) on suppression inputs meeting neither path. NO 'verdict'-kind conviction check was added: kind 'verdict' is a window-EVIDENCE record with no conviction claim (the body carries no convicted flag; conviction is the consumer's windowVerdict/lifetimeVerdict threshold predicate, and sub-threshold per-window records are load-bearing J7 lifetime evidence — the suite's own canonical verdict fixtures sit at 4.14σ < 5σ, "escalate-not-convict"), so gating zMicro ≥ zThresholdMicro would reject the documented evidence class. No PARAMS_A5 change (no digest drift), no anchors/corpus/golden changes.

**Verified.** New deterministic regressions in scripts/test-accounts-tier2.mjs (synthetic entries, no engine): an honest 30-entry sub-escalation suppression (z = −551,207 — the exact probe scenario) is now REJECTED with the typed trigger error (was accepted pre-fix); a 3-entry hand-picked suppression is REJECTED even at z = 5,196,304 ≥ 3σ (trailing-K geometry required); makeTier2Verdict THROWS on both and on a neither-path case (sub-escalation window + zLife = 2,600,000 lifetime); the genuinely-escalating full-K suppression fixture (z = 3,411,623 ≥ 3e6) still builds and verifies (round-trip), a lifetime-fired suppression (window sub-escalation, zLife = 3,677,510 ≥ 3e6) builds + verifies, and a forged sub-escalation-lifetime suppression is REJECTED. Pre-existing suppression fixtures already used escalating trailing-K windows — none needed strengthening. Suites green: test-accounts-tier2.mjs (263 asserts, all pre-existing goldens intact), test-accounts-tier1.mjs (174), npm run typecheck.

**Residual.** *[Superseded 2026-07-22 by the A5-21 decision: the suppression gates this fix installed now enforce the 5σ CONVICTION (`zThresholdMicro`), and the error string is `'suppression conviction did not fire on the recomputed evidence (…)'` — the recomputed-from-inputs discipline this entry established is unchanged, only the line moved. See A5-21.]* The two chain-side claims — that `entries` really are the accused ladder's trailing-K window at the named ordinal, and that no selfban precedes deadlineEvent — remain the auditor's chain-bytes duties by design (verifier header contract).

---

## Major (16)

### A5-04 — [FIXED] Judge-adapter liveness contract (onError makes an in-flight judgeGame fail instead of hang) has zero coverage on both adapters

- **File:** `scripts/test-judge-node.mjs`  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** Test-only — no adapter behavior changed (the onError contract is already correct on both adapters: node via nodeAdapter.ts `onError = inst.onExit(...)`, web via the A5-23 `makeWorkerJudgeEngine` teardown). The finding was filed against the browser fixture's close-after-judge gate (`accounts-fixture.mjs:519`); the coverage now lives in test-judge-node.mjs, engine-free, on BOTH adapters. The **web half** is the pre-existing §A5-23 section (real `judgeGame` over a spec-faithful fake Worker: close-in-flight → JudgeEngineError, post-close `send()` throws, worker `error` → JudgeEngineError). This fix adds the **SYMMETRIC node section** that was the true gap — no prior test ever killed a child mid-judgeGame, so the `nodeEngine child-exit → nodeAdapter onError → judge-core JudgeEngineError` path had zero coverage while the web hang shipped. It drives the REAL `newNodeJudgeEngine` + REAL `judgeGame` over a tiny fake UCI glue (answers only the handshake/`isready` barriers; on `go` STALLS to model a search in flight, or after `__autogo__` auto-replies MultiPV+bestmove; `quit`→exit 0, `__die__`→exit 1). The glue never loads its `.wasm` sibling — that sibling is a byte-copy of the pinned wasm present ONLY so both §8 gate layers pass and the SHIPPED adapter (not a hand-rolled double) is exercised end to end; no Stockfish runs and nothing is judged. Four cases: positive control (full judgeGame completes + clean close), `close()` mid-flight → JudgeEngineError, judgeGame on an already-closed engine → rejects via the fail-closed `send()` throw ("judge engine has exited"), and abrupt child crash mid-flight → JudgeEngineError + post-death `send()` throws. No PARAMS_A5 change, no golden re-freeze.

**Verified.** New engine-free node section in test-judge-node.mjs (+7 asserts): in-flight `close()` and abrupt crash both reject the in-flight judgeGame with `JudgeEngineError` ("judge engine failed mid-game: judge engine process exited"), the closed-engine judgeGame rejects via the `send()` throw, and post-death `send()` throws — each raced against the existing `withTimeout` hang-guard so a regressed (hanging) path FAILS loud (red `TIMEOUT:…`) instead of stalling the suite. Non-vacuity confirmed out-of-band: an adapter object that OMITS onError leaves the in-flight/crash cases HANGING (the guard fires, `err.name` ≠ JudgeEngineError → red), so the assertions genuinely catch the defect. Deterministic across reruns. Nothing else moved — the FROZEN Tier-1 digest `F7XHAGNBkIR_2l6H7J8-Y2JTOOFj5SBtyYGvPAFIl3s` still reproduced bit-for-bit (adapters/core untouched), wasm goldens intact. Suites green: test-judge-node.mjs (78 asserts, was 71), test-judge-fit.mjs (68), operator-smoke.mjs (20; owned fixture's `JUDGE_PARITY_POSITIONS` + judge onError/close surface preserved), npm run typecheck (all three tsconfigs).

**Residual.** The node section deliberately routes a fake UCI glue behind a satisfied §8 gate (real-wasm sibling) to stay engine-free and deterministic: it exercises the adapter LIFECYCLE (child-exit → onError → JudgeEngineError), NOT judging correctness — the latter stays the real-engine FROZEN-digest gate elsewhere in the same suite. This does not weaken A5-13: the checked-vs-loaded / TOCTOU matrix (real glue copy + tampered sibling) is a separate section here, untouched. The web half remains under the adjacent §A5-23 banner (the A5-23 adapter fix's own regression), so A5-04's web coverage depends on that section persisting. The mobile memory-pressure worker-kill sub-scenario (§8/§11 name phones as a judge platform) is browser-only and not reproducible in a headless node suite — left to the A6 browser-wiring pass, but the onError path it would exercise is exactly the one now covered on both adapters.


### A5-06 — [FIXED] tier1Record fail-closed matrix has no wrong-(nodes, multiPv, hashMb) case — a Tier-2-config or nodes=1 'Tier-1' record is mintable with every suite green

- **File:** `scripts/test-accounts-tier1.mjs`  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** The root-cause code hole — tier1Record gated only the params digest, never the (nodes, multiPv, hashMb) — was closed by **A5-37**, which landed the canonical fixed-node config gate in tier1.ts (config must equal judgeConfigForTier(1) = t1Nodes/t1MultiPv/hashMb, else Tier1InputError) together with its own §(j) mint-time regressions (the Tier-2 2M/MPV6 output, the degenerate nodes=1/MPV6/Hash1 output, and each of nodes/multiPv/hashMb isolated — all REJECTED; canonical config still ACCEPTED with a byte-identical tier1Digest). A5-06 is the test-coverage residual: a new §(k) in test-accounts-tier1.mjs (landing AFTER §(j); tier1.ts untouched) adds the two regressions §(j) does not carry. (1) The finding's LITERAL scenario — a nodes=1 "near-random" output with multiPv/hashMb left CANONICAL to look compliant (§(j)'s nodes=1 case bundles MPV6/Hash1) — is REJECTED at the isolated nodes gate. (2) The END-TO-END closure of the finding's "the record drops the config … no suite contains an assertion that could ever flag this record class": the Tier1Record body carries no raw nodes/multiPv but COMMITS to the whole JudgeOutput (config included) through `judge` = judgeOutputDigest(out), so config is never erased from the verdict trail — a nodes=1 output digests DIFFERENTLY, and because a producer's mint and a verifier's recompute are the same tier1Record(out) call, that recompute re-runs the A5-37 gate and throws, making the wrong-config record class both unmintable and unreproducible. No tier1.ts change (the gate already exists), no PARAMS_A5 touch, no golden re-frozen.

**Verified.** New deterministic §(k) regressions (synthetic JudgeOutputs, no engine): tier1Record over `{…FIX_A, config:{…CFG, nodes:1}}` (multiPv 4 / hashMb 16 canonical) throws Tier1InputError with the "(nodes 1, multiPv 4, hashMb 16) is not the Tier-1 config" gate message — the finding's mintable-pre-A5-37 scenario, now fail-closed; `recCanon.judge === judgeOutputDigest(FIX_A)` (the record commits to the exact output); `judgeOutputDigest({…FIX_A, config nodes:1}) !== judgeOutputDigest(FIX_A)` (config is covered by the judge digest, not erased); `!('config' in recCanon)` (the record body carries no config field, so tier2's mkRec legitimately carries none — the (nodes,multiPv) contract lives at the tier1 mint/recompute boundary, not in the record or the Tier-2 layer). Suites green: test-accounts-tier1.mjs (268 assertions, up from 264; §(j) A5-37 cases and every frozen tier1Digest golden intact), test-accounts-tier2.mjs (263, goldens intact), npm run typecheck (node + web + server).

**Residual.** The tier2 mkRec "never exercised end-to-end" observation (scripts/test-accounts-tier2.mjs:180-188, out of this lane) is non-actionable for config enforcement: Tier1Record has no config field by design — it commits to config via `judge` — so tier1Record mint/recompute is the sole enforcement point (covered in §(k)); a tier1→windowVerdict integration test would add no config-gate coverage because windowVerdict never sees config. The ANCHOR side of the (nodes,multiPv)-match contract — TIER2_ANCHORS_JUDGE fitted at 200k/MPV4 having to match the judged output's config — is an anchors.ts/J6 concern outside the tier1 lane.


### A5-07 — [FIXED] Suppression receipts have zero negative coverage: no sub-escalation suppression case, and the header-claimed `kind` tamper assertion does not exist

- **File:** `scripts/test-accounts-tier2.mjs`  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** Test-coverage finding (verifier note: "a kind tamper WOULD fail verify today … header-integrity/coverage gap rather than a code bug"). The A5-03 fix already makes the source reject a sub-escalation suppression (`verifyTier2Verdict`/`makeTier2Verdict` enforce the recomputed §8 trigger), and a `kind` tamper already flips verify false (schema refine + signature) — so the gap was the SUITE's missing assertions, not a code defect. Added, all in `scripts/test-accounts-tier2.mjs` (NO source / PARAMS / anchors / golden / digest change): (1) the header-claimed but absent tamper-matrix case `mut({kind:'suppression'})` on the canonical verdict record ⇒ verify false (a suppression body without a `deadlineEvent` is schema-invalid); (2) an A5-07 block pinning that `kind` is LOAD-BEARING — the SAME honest sub-escalation window (`w30(inno)`, z = −551,207 ≈ −0.55σ, the finding's exact shape) is a VALID plain `'verdict'` yet an INVALID `'suppression'` (the §8 trigger never fired), so flipping ONLY the kind flips verify, locking the recomputed z ≥ zEscalateMicro suppression check against silent regression (the failure scenario's "no existing assertion locks it in"); (3) a schema-legal `suppression`→`verdict` kind flip (deadline dropped so `'verdict'` is valid, statistic recomputes identically) still breaks the record signature — the rigorous integrity form of 'kind ⇒ verify false', proving `kind` is inside the signed body; (4) the trailing-K geometry tie the suite never asserted — a valid suppression's `entries.length === reganK` and its record lists all K game keys (complementing the pre-existing `supp3` negative that a ≠ reganK window is refused even at z ≥ 3σ).

**Verified.** 6 new deterministic assertions (synthetic fixtures, no engine), all green: `mut({kind:'suppression'})` ⇒ verify false; `verdictInno` verifies as a plain verdict while the identical-window `suppInno` (z = −551,207) is REJECTED; the schema-legal kind flip breaks the signature; `trailing.length === reganK` (30) and `supp.body.games.length === K`. Suites green — test-accounts-tier2.mjs (328 assertions, all pre-existing goldens intact), test-accounts-tier1.mjs (268), test-accounts-ratings.mjs (300, bundles fold.ts), and `npm run typecheck` (node/web/server).

**Residual.** None in-lane. The chain-side claims delegated by design — that `entries` really are the accused ladder's trailing-K window at the named ordinal, and that no selfban precedes `deadlineEvent` — remain the auditor's chain-bytes duties (verifier header contract). *[Superseded 2026-07-22: A5-21 re-anchored the suppression check this entry's assertions lock (now z ≥ zThresholdMicro, and the kind-flip fixtures were re-cut onto convicting windows); the load-bearing-kind and signed-body properties this entry pinned carry over unchanged.]*


### A5-08 — [FIXED] Fold-bans tests pin unconditional 'newest wins' with only growing expiries — the un-ban path and the deterministic-deadline validation have no test anywhere

- **File:** `scripts/test-accounts-tier2.mjs`  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** A5-08 is a test-coverage finding; the fold behavior it feared was already made correct earlier in this lane by A5-22 (self-ban expiry DERIVED from the event's witnessed ts + §9 term, folded MONOTONICALLY so a convict cannot self-un-ban) and A5-38 (`bans` bounded to played ladders) — banStep (`fold.ts:369-378`) is untouched here. The named coverage gaps are now closed by a dedicated A5-08 regression in section 7 (right after the A5-22/A5-38 blocks): (1) the deterministic §8/§9 deadline the fold-bans suite lacked — a folded selfban's `until` equals `selfBanExpiryWts(event ts)` EXACTLY (the analogue of the PIN-fuse expiry check); (2) the finding's LITERAL un-ban scenario — a compliant 90-day selfban followed by a second `selfban{expiryWts:1}` does NOT erase the ban (its `until` folds to `selfBanExpiryWts(its event ts)`, NEVER the payload 1), and both read surfaces the finding names stay correct end-to-end (`displayState` still `banned`, `pairingLegal` still illegal `'banned'`); (3) newest-wins ONLY under monotonic DERIVED expiry — the original defect's own direction inverted: a second selfban carrying a HUGE payload `expiryWts` but a NON-advancing event ts does NOT extend the ban (the payload number is inert; only the derived `until` wins), and an equal derived `until` (same event ts) is a no-op too (first-wins on the `prev.until >= until` tie). Also corrected the suite header summary (g), which still described the fold as "newest wins" — the pre-A5-22 language A5-08 flags — to "expiry DERIVED from the event ts + §9 term and folded MONOTONICALLY … bounded to PLAYED ladders." No source change (fold.ts/tier2.ts untouched); no golden constant added or changed (every value derives from `selfBanExpiryWts` + synthetic events with dummy sigs), so nothing re-frozen.

**Verified.** 8 new deterministic assertions (no engine), test-accounts-tier2.mjs 328 → 336 green. Regression proven genuine: temporarily reverting banStep to the pre-A5-22 `until := payload.expiryWts` overwrite flips 6 of the 8 A5-08 assertions red — the un-ban `until` folds to 1 (not the derived deadline `1710368000000`), `displayState` returns `"ranked"` not `"banned"`, `pairingLegal` returns legal not `'banned'`, the huge-payload `until` = `9999999999999` and `window` = 9 are adopted, and the tie `window` = 7 is adopted — alongside the A5-22 block flipping red, then fold.ts was restored byte-for-byte (no scratch marker remains). Suites green: test-accounts-tier2.mjs (336), test-accounts-ratings.mjs (300, fold.ts goldens un-refrozen), test-accounts-tier1.mjs (268), npm run typecheck (all three tsconfigs).

**Residual.** The chain-level observation A5-08 also raised — chain.ts has no `'selfban'` rule and no chain-suite test admits/rejects a duplicate/shrinking selfban — is out of this lane (`src/shared/accounts/chain.ts` / `scripts/test-accounts-chain.mjs`) and is NOT required for correctness: the honest verifier's a4-v1 fold is now robust standalone (a duplicate or shrinking selfban is a deterministic no-op regardless of what verifyChain admits, so every verifier reaches the same right answer). A belt-and-suspenders chain.ts "one active selfban per ladder, non-shrinking" admission rule remains an optional defense-in-depth hardening for a future chain-lane pass.


### A5-09 — [FIXED] Honest zero-FPR proof is cross-sectional and circular — it structurally cannot exhibit the persistent per-account anchor-residual offset the production longitudinal trigger accumulates on (holds even at elo == trueElo)

- **File:** `scripts/test-judge-calibration.mjs`  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** The §3 honest holdout was cross-sectional (every trailing-K window is 30 DIFFERENT corpus bots at different bands, and the anchors are the PAV band-means of those same sides ⇒ every honest deviation is mean-zero by construction), so it structurally could not exhibit the ONE geometry escalationDue evaluates in production: a single account's trailing reganK stream. A deterministic LONGITUDINAL single-account holdout was added to §3 (committed corpus only, NO re-judging): it assembles ONE honest account of fixed TRUE strength T = 2700 (the top honest band) from the committed T-band honest sides, cycled over N = 3·K = 90 games, on a COUPLED climb where the fold RD decays 350 → 30 and the DISPLAY rating lags true strength via `elo = T − 2·RD` (700 → 60 Elo of lag) while `effElo = elo + RD_CONF_MUL·RD ≡ T` at every game. The stream is scored two ways through the SHIPPED escalationDue/aggregateZMicro: `legacyEntry` (rdMicro absent = the pre-A5-02 point-estimate path) and `rdEntry` (rdMicro present = the deployed A5-02 path). This turns the §14-A5 evidence artifact into a live regression on the A5-02 RD-aware fix (which threads `rdMicro` into gameDevMicro). No PARAMS_A5 / anchors / corpus / golden change.

**Verified.** `CI=1 node scripts/test-judge-calibration.mjs` ALL GREEN — 73 assertions (was 58); two CI runs byte-identical. The longitudinal true-2700 climb: legacy (rdMicro absent) max trailing-30 z = 4.550σ ⇒ `escalationDue.due === true` — the honest account FALSELY escalates at its lagging display rating (the A5-02 false positive the cross-sectional §3 windows cannot produce); RD-aware (A5-02 effElo) max trailing-30 z = 0.503σ < 3.0σ ⇒ `escalationDue.due === false` — the SAME stream never fires, so the display-rating-lag longitudinal FPR is closed and the fix is proven non-vacuous (the legacy branch is the counterfactual that would have convicted). Pre-existing §3 cross-sectional guards intact (max honest trailing-30 z = 1.482σ, split-half 2.123σ). `npm run typecheck` green (node/web/server).

**Residual.** This holdout closes the display-rating-LAG longitudinal FPR (A5-02). The finding's OTHER mode — a SETTLED account whose per-game move quality persistently sits d·σ off its true-band mean (a within-band style residual the RD fix cannot compensate once RD floors, aggregating by √n to z ≈ d·√K) — is NOT exhibited here and is documented in-code: the committed corpus has a single play-model per band ⇒ ZERO within-band variance, so it cannot represent such a player. Proving that mode safe needs a heterogeneous corpus with within-band variance (engine regeneration, which drifts anchors/goldens) or per-player baselining in tier2.ts (cross-lane) — deferred to the pre-ship [A5-CALIBRATED] recalibration. The deterministic machinery itself is unchanged and correct.


### A5-10 — [FIXED] Engine-identity guard for the J3 fit is vacuous: F.wasmSha256 does not exist, so the assertion compares PARAMS_A5.judgeWasmSha256 to itself — a judge-engine swap slips past the fit suite

- **File:** `scripts/test-judge-fit.mjs`  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** The wasm-identity leg now genuinely BINDS. test-judge-fit.mjs freezes the fit-time judge wasm sha256 as an independent golden literal `FIT_WASM_SHA256 = a8fbc0…96f1` — the binary the J3 corpus was judged under (folded into the fit-time `JUDGE_ANCHORS_PARAMS_DIGEST` kkHG…, still the live `PARAMS_A5.judgeWasmSha256`; test-judge-node cross-checks that exact literal against the shipped blob AND the derived contentHash pin, so it is a recorded provenance fact, not a fabrication) — and asserts the LIVE `params.PARAMS_A5.judgeWasmSha256` equals it through a single reused comparator `wasmMatchesFit`. Because the two operands come from INDEPENDENT sources (a frozen literal vs the live import) rather than both reading PARAMS_A5, a judge-engine re-pin (new build → new judgeWasmSha256 → PARAMS_A5_DIGEST drift, corpus stamps migrated per J7) moves the live pin off this freeze and FAILS the assertion, forcing a J3 re-fit — the swap can no longer slip past under the non-fatal benign-drift note (which still, correctly, only fires for non-engine rows like lifetimeScheme; the digest legitimately drifted kkHG…→a4eN… today with the wasm unchanged). This provenance freeze re-freezes ONLY together with a J3 corpus re-fit, unlike test-judge-node's `GOLDEN_WASM_SHA256` which tracks the shipped blob and re-freezes on ANY re-pin — so a re-pin-without-re-fit leaves the two diverged, which is exactly the tripwire. The drift-rule comment (198) was corrected to name the golden freeze instead of the never-binding when-present check; the A5-12 when-present block is kept only as a forward-compat cross-check for the day a J3 re-fit persists a wasmSha256 field on the artifact. The fit JSON / JUDGE_ELO_FIT (anchors.ts) were NOT touched (out of this suite's lane) and NOTHING was re-judged: no PARAMS_A5 change, no digest drift, no golden/anchor/corpus re-freeze.

**Verified.** `node scripts/test-judge-fit.mjs` ALL GREEN — 68 assertions (up from the 66 the post-A5-12 note-only state emitted; +2 = the now-binding live-pin assertion and an A5-10 non-vacuity regression). Non-vacuity proven two ways: (1) in-suite, `wasmMatchesFit('deadbeef'×8)` — a re-pinned engine's sha256 — is REJECTED while the live pin passes, exercising the SAME comparator the guard uses; (2) suite-level swap probe (throwaway `scripts/.a5-10-probe.mjs`, since removed) that diverges the frozen literal from the live pin — the engine-identity live-pin assertion flips ✗ and the suite exits ❌ (2 FAILED), whereas pre-fix the `F.wasmSha256 ?? PARAMS_A5… === PARAMS_A5…` leg was green for ANY wasm value. `npm run typecheck` green all three tsconfigs (node/web/server). test-judge-node.mjs (engine-heavy) was not re-run: neither it nor its bundled TS (nodeAdapter/nodeEngine/judge/contentHash) was edited, so its result is unaffected and re-running it is outside the deterministic no-engine verify scope.

**Residual.** The artifact still does not SELF-carry a wasmSha256 field: persisting it on `JUDGE_ELO_FIT` + `judge-elo-fit.json` is an anchors.ts / scripts-data change outside this suite's lane and belongs to the next J3 re-fit — the forward-compat `if (F.wasmSha256 !== undefined)` cross-check is already in place to lock it the day it lands. anchors.ts:22-25's claim ("the suite compares it against the current digest and fails loudly on drift") remains imprecise — benign digest drift correctly stays a note and only the engine-identity dimension fails loudly — and should be tightened by the anchors owner (out of lane), but no mechanical gap remains. The other engine-swap backstop (the re-judge receipt in test-judge-calibration §7) is still skipped under CI by design; this suite is now the CI-visible tripwire.


### A5-12 — [FIXED] Judge WASM hash pinned in two unlinked literals (PARAMS_A5.judgeWasmSha256 vs contentHash.ts JUDGE_WASM_SHA256) with no equality assertion anywhere

- **File:** `server/judge/contentHash.ts:33`  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** The second literal is gone: contentHash.ts now imports the shared params module and DERIVES the node pin — `export const JUDGE_WASM_SHA256 = PARAMS_A5.judgeWasmSha256` — so the node gates (nodeAdapter/nodeEngine/peer via verify/assertWasmHash), the web gate (src/web/engines/judge.ts), and the params echo attested by every JudgeOutput/Tier1Record all enforce the ONE copy folded into PARAMS_A5_DIGEST; divergence is impossible by construction, and a binary re-pin now REQUIRES editing params.ts, which drifts PARAMS_A5_DIGEST so every verdict names the new rule set (§8/§0). Server → shared is the allowed import direction, and every consumer of server/judge already resolves `@shared` (verified individually: esbuild aliases in test-judge-node/gen-judge-corpus/gen-cheater-corpus/test-judge-calibration/operator-smoke, accounts-fixture bundleFixture, tsconfig.server paths; the production dist-server bundle imports no judge/operator code, so build-server.mjs is unaffected). JUDGE_WASM_BYTES stays a contentHash-local literal by design — the independent length cross-check that canonical PARAMS_A5 cannot carry without a forbidden digest drift; forgetting it on a re-pin fails the node gate loudly, never silently. test-judge-fit's wasm-identity check — which the verifier showed compares PARAMS_A5 with itself (`F.wasmSha256 ?? PARAMS_A5.judgeWasmSha256`; the artifact has no such field) — no longer green-lies: it asserts only when the field exists and otherwise prints a loud persist-at-next-J3-re-fit note. No PARAMS_A5 edit, no digest drift, no golden re-freeze anywhere.

**Verified.** New A5-12 regressions in scripts/test-judge-node.mjs §(3) (runs in CI — build.yml executes this suite — and before any engine section): `content.JUDGE_WASM_SHA256 === core.PARAMS_A5.judgeWasmSha256` (catches a revert to an independent literal) and shipped-wasm `v.sha256 === core.PARAMS_A5.judgeWasmSha256` (catches the exact failure scenario: blob + this file's goldens re-pinned, params.ts stale — previously green through all CI). Negative proof, engine-free: a byte-flipped wasm vs the unchanged attested pin flips the new assert red with verifyWasmHash ok=false (fail-closed), and a synthetic contentHash copy re-pinned to a diverged literal flips the equality red; the untouched green path holds. Values unchanged end-to-end: frozen Tier-1 digest `F7XHAGNBkIR_2l6H7J8-Y2JTOOFj5SBtyYGvPAFIl3s` reproduced bit-for-bit, wasm goldens intact, tampered-wasm gate still throws typed JudgeWasmHashError. Suites green: test-judge-node.mjs (44 asserts, was 42), test-judge-fit.mjs (66 asserts; the one formerly-vacuous unconditional assert is now an honest conditional, −1), npm run typecheck (all three tsconfigs).

**Residual.** The fit artifact still persists no wasmSha256 — regenerating it is engine work out of this budget; until the next J3 re-fit writes the field, the FIT's wasm identity rests on the corpus judgeParams pin (the fit-time PARAMS_A5_DIGEST folds judgeWasmSha256), which the suite now states loudly instead of asserting vacuously. The CI skip on the browser engine-parity gate (test-web-accounts-browser.mjs:282-283, outside this lane) is unchanged, but the A5-12 class no longer depends on it: the two pins cannot diverge structurally and blob-vs-attested-pin drift is asserted in CI via test-judge-node. The node gate's checked-vs-loaded/TOCTOU split is the separate finding A5-13, untouched here.

### A5-13 — [FIXED] Node content-hash gate verifies a file the engine never loads: checked-vs-loaded split plus TOCTOU (web blob-URL path is immune)

- **File:** `server/judge/nodeAdapter.ts:30`  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** The gate now hashes the EXACT `.wasm` the spawned child loads. New `loadedWasmPath(enginePath)` in nodeEngine.ts mirrors the shipped glue's Node (`require.main===module`) branch byte-for-byte — `path.join(dirname(enginePath), basename(enginePath, extname)+'.wasm')`, the sibling the child self-resolves and `readFileSync`s (the parent never hands it a wasmPath). Both entry points route through new `gatedWasmPath(enginePath, wasmPath?)`: it returns that loaded sibling and, when the caller supplied an explicit `wasmPath`, REFUSES fail-closed with a new typed `JudgeWasmPathError` (exported from nodeEngine.ts) unless the path resolves to that same sibling — a wasmPath decoupled from the executed file is never silently trusted. newNodeJudgeEngine (nodeAdapter.ts) hashes `gatedWasmPath(...)` at layer 1 (JudgeWasmHashError on a byte mismatch) and now spawns via `newInstance({ enginePath })` with no external wasmPath, so layer 2 re-derives + re-verifies the SAME loaded sibling immediately before spawn (`assertWasmHash(gatedWasmPath(...))`), minimizing the check->load window. This is the node analogue of the web adapter's verified-bytes `blob:` URL (src/web/engines/judge.ts): the file we verify is the file the engine executes. The misleading "pass an explicit wasmPath if the sibling is elsewhere" guidance — which the subprocess can never honor — is gone from both docstrings. No PARAMS_A5 change, no digest drift; every in-repo caller passes `wasmPath == enginePath`'s sibling, so gatedWasmPath returns that sibling un-thrown and the verified/executed bytes are byte-identical to pre-fix — no golden re-freeze.

**Verified.** Rewrote the J1 content-hash section in scripts/test-judge-node.mjs into an A5-13 gate suite (engine-free — every refusal throws BEFORE any child spawns): `loadedWasmPath(ENGINE_JS)` equals the shipped sibling and `verifyWasmHash(loadedWasmPath(...)).ok` (happy path); the DIVERGENT-PAIR regression — a glue copy whose OWN sibling is tampered, gated by a PRISTINE wasmPath — is now REFUSED with typed JudgeWasmPathError (this was GREEN pre-fix: the gate hashed the pristine wasmPath while the child would load the tampered sibling and mint un-pinned bits); a tampered loaded sibling with NO wasmPath is REFUSED with typed JudgeWasmHashError (the gate hashes the actual loaded file); a decoupled wasmPath (pristine sibling + tampered temp) is REFUSED with JudgeWasmPathError (never silently ignored). An engine-free esbuild probe confirmed the discrimination directly: the OLD logic reports `verifyWasmHash(pristine).ok=true` while `loadedWasmPath(eng13)` is the tampered `eng13.wasm` (checked != loaded — the exact defect), and `gatedWasmPath` with a matching wasmPath / undefined passes through to the loaded sibling (back-compat preserved). Suites green: test-judge-node.mjs (50 asserts, was 44; FROZEN Tier-1 digest `F7XHAGNBkIR_2l6H7J8-Y2JTOOFj5SBtyYGvPAFIl3s` reproduced bit-for-bit, wasm goldens intact), test-judge-fit.mjs (66, unaffected), npm run typecheck (all three tsconfigs). Not re-run (engine-heavy / re-judges games, byte-unaffected because they pass wasmPath == the sibling): test-judge-calibration.mjs (newNodeJudgeEngine at :528) and operator-smoke.mjs / peer.ts.

**Residual.** The checked-vs-loaded split — the confirmed attacker-free harm — is fully closed. A narrow check->load TOCTOU window remains: node cannot hand in-memory verified bytes to a `node <glue>.js` child because the glue's Node/`require.main` branch exposes no `wasmBinary`/`locateFile`/hash override (only the WORKER branch's `#<url>` hook the web adapter exploits), so full byte-immunity would require reimplementing the Emscripten loader/REPL in-process to feed `Module.wasmBinary` — out of this lane's budget and a determinism-golden risk. The window is minimized (verify the loaded sibling immediately before spawn) and the residual local-write race is caught by A5's re-run/tamper-evidence model (a swapped binary yields divergent output on honest replay), matching the finding's own accepted remedy ("verify the exact resolved sibling the child loads") and the verifier's note that the TOCTOU sub-claim alone is marginal under tamper-evidence philosophy. The A5-11 test-gap ("verified bytes are the executed bytes" untested) is now covered by the divergent-pair regression above.


### A5-14 — [DEFERRED] Score-equivalence window is dead for in-list moves: list membership alone certifies an engine match (rank-4 self-match), and out-of-list blunders match any line's score

- **File:** `src/shared/accounts/judge/tier1.ts`  ·  **Severity:** major  ·  **Status:** DEFERRED — **RATIFIED 2026-07-22** (owner-delegated): flipping the match criterion without the J6 anchor refit would break the calibrated honest null — a direct false-positive hazard, forbidden under "honest players are never banned". Revisit at the J6 refit event, scheduled before A-final volume (the entry's promotion conditions stand).

**Fix.** Minimal safe change only — the behavioral switch is calibration-coupled (see Residual). tier1.ts now implements the §8-intended BEST-relative criterion alongside the calibrated one: new exported `isEngineMatchedBest(playedCp, lines)` — matched iff |playedCp − rank1Cp| ≤ PARAMS_A5.scoreEquivCp (same ±15cp width, NO new parameter; rank-≥2 lines are validated fail-closed but never widen the criterion) — and `SideMoveScores.matchedBest`, computed by sideMoveScores next to `matched`. Under this criterion the window is live in BOTH directions for EVERY move and it is still never exact-move matching (a rank-≥2 or unlisted move within ±15cp of best matches; matchedBest ≤ matched structurally since the best line is among the lines). The calibrated any-line criterion behind matched/matchMicro is byte-for-byte UNCHANGED — it is the statistic the MEASURED anchors (anchors.ts matchByElo/sigmaMatchMicro) were fit under, and scoring the corrected statistic against those anchors would break the J6 null. matchedBest is deliberately diagnostics-level only — NOT in Tier1Side/Tier1Record and never a Tier-2 input: promoting it into the record now would fork the digest-frozen record shape that the stored J6 corpus engine receipt pins bit-for-bit (test-judge-calibration.mjs §7). The tier1.ts header carries an [A5-14 DEFERRED] block documenting the confirmed degeneration and the exact promotion conditions; Tier1Side's docstring pins the deliberate absence.

**Verified.** New deterministic regressions in scripts/test-accounts-tier1.mjs §(b2) (synthetic fixtures, no engine) reproduce the finding's exact failure scenarios and prove the corrected criterion catches them: (i) {r1 a2a3 +200, r2 b2b3 +120, r3 c2c3 +40, r4 d2d4 −150} played d2d4 → losses [350], matched 1 (the confirmed defect, pinned as such) but matchedBest 0; (ii) unlisted g2g4, ground truth −140 → matched 1 via the rank-4 window (|−140 − (−150)| = 10 ≤ 15) but matchedBest 0 (|−140 − 200| = 340); (iii) fully-listed 3-legal-move position, worst move (950cp loss) → matched 1, matchedBest 0 — composition no longer forces matches; the window is LIVE for listed moves (in-list rank-2 at 16cp: matched 1 vs matchedBest 0) yet never exact-move (in-list rank-2 at 10cp and an unlisted above-best ground truth +205 vs best +200 both best-matched); inclusive 15/16 boundary, mate-band conservatism, matchedBest ≤ matched invariant on FIX_A (w 1/2, b 1/2); FIX_C — the A5-05-cited matched=10 "in-list at any rank" golden — now discriminates matchedBest 5/10 for the 150cp-ACPL white (five 300cp rank-2 self-matches stripped) vs 10/10 honest black. Record surface untouched: Tier1Side key set frozen by assert, matchedBest provably absent from both record sides, and the pre-existing frozen tier1Digest golden (`chzY1umBfAfE9M6Ce6u_73xtvCaMEIwOK7dw_mKVm-8`) still passes byte-identical. Fail-closed matrix extended (float playedCp, empty lines, malformed non-best line refused). Suites green: test-accounts-tier1.mjs (204), test-accounts-tier2.mjs (263, goldens intact), test-judge-calibration.mjs non-engine sections under CI=1 (55; the §7 engine receipt is unaffected by construction — record bytes unchanged), npm run typecheck (all three tsconfigs).

**Residual.** The behavioral switch is deferred to ONE coordinated lead-level event (the J6 anchor refit), because an in-lane flip is either ineffective or calibration-breaking: cheater separation is (observed − matchByElo)/sigmaMatchMicro, so changing tier1 semantics WITHOUT refitting the anchors gains zero power (an engine-best cheater's matchMicro stays ~1e6 under both criteria and the expected curve is unchanged) while pushing every honest devMatch negative — breaking the measured z ~ N(0,1) null that the 5σ FPR bound and the lifetime scheme rest on. The event must bundle: (1) J6 re-measurement of matchByElo/sigmaMatchMicro on the honest corpus under the best-relative statistic (engine corpora — out of this pass's budget; anchors.ts is outside this lane); (2) promotion of matchedBest/matchBestMicro into Tier1Side + tier2 consumption (tier2.ts out of lane; its checkTier1Side tolerates the extra field, so the shape change is additive); (3) the coordinated Tier1Record re-freeze — this suite's key-set/digest pins and the stored-corpus engine receipt (test-judge-calibration §7) all fail loudly on that change, forcing a conscious re-freeze; (4) the ACCOUNTS-PARAMS.md §Judge score-equivalence row update (lead-owned doc). Until then: records minted today carry only the any-line statistic — nothing is permanently lost (Tier1Records are pure functions of transcript + judge and recomputable per §8), but deriving matchedBest for pre-switch games costs a T1 re-judge, which argues for scheduling the event before A-final volume. Interim exposure is bounded and unchanged from the confirmed analysis: the honest null holds because the anchors were fit under the same degenerate statistic (max honest trailing-30 z 1.884σ); the cost is compressed high-Elo cheater separation (~0.98σ max devMatch at 2700) and composition-driven positive pressure for low-branching-heavy honest players. The corrected criterion's semantics are now frozen by regression, so the refit lands on pinned behavior rather than a re-derivation.

### A5-15 — [FIXED] Fischer-increment/byo-yomi credit is baked into the signed snapshots, so honest fast play yields clockFitMicro = 0 — the hardcoded 'machine pacing' maximal-suspicion value

- **File:** `src/shared/accounts/judge/tier1.ts`  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** `clockSamplesForSide` now credits the game's Fischer increment BACK before the ≥0 clamp: `t_i = min(1e9, max(0, clockMs[i−1][s] − clockMs[i][s] + incMs))`. The signed snapshot is taken AFTER `afterMoveCredit` (mpSession), so on a main-time move the raw delta `before−after = elapsed − incMs`; adding `incMs` recovers the true elapsed think. It is EXACT for pure-Fischer play — every sampled ply (i ≥ 1) was credited the increment, and the only no-increment move (the opening ply 0, mpSession `isOpeningMove`) is never sampled. `incMs` is the witness-signed `RatedBinding.tc.incMs` (segment.ts) — the only verified time-control datum the derivation needs — threaded as an OPTIONAL param `clockSamplesForSide → tier1Side → tier1Record`, defaulting to 0; absent ⇒ byte-for-byte the pre-A5-15 delta, so every frozen Tier1Record/digest holds un-refrozen. It is a derivation INPUT only, never stored (Tier1Record/TranscriptMove shape untouched, no digest drift): a verifier recomputes from the same signed tc. Fail-closed — `incMs` is validated as a non-negative safe integer ≤ `CLOCK_INC_MAX_MS` (3_600_000, a LOCAL const mirroring the signed `incMs` schema bound, NOT a PARAMS_A5 edit → no digest drift). The T=0 branch is thereby restored to its documented meaning: with the increment credited back, an honest sub-increment human's thinks are > 0 (proportional fit), while an actual instant bot still nets 0 → still maximally flagged — the aliasing is broken in the fail-safe (§0) direction. No PARAMS_A5/anchors/corpus/golden change.

**Verified.** New (d2) regressions in scripts/test-accounts-tier1.mjs (synthetic, no engine): an 18-ply honest 3+2 game (think ∝ complexity, every elapsed < 2000ms increment) reproduces the finding's aliasing — pre-fix BOTH sides `clockFitMicro=0` (maximal suspicion) and the record is BIT-IDENTICAL to an instant-0ms bot; with `incMs=2000` credited back the human recovers to `1_000_000` (proportional, not flagged) while the bot stays pinned at 0, and the two now mint DISTINCT records (aliasing broken, honest not whitewashing the bot). `clockSamplesForSide` thinkMs recovered exactly (`[0×8] → [360,1800,360,1800,…]`). Byte-identical default proven three ways: `incMs` absent ≡ explicit 0 (record bytes unchanged); the frozen full-coverage tier1Digest `chzY1umBfAfE9M6Ce6u_73xtvCaMEIwOK7dw_mKVm-8` reproduced; test-accounts-tier2.mjs 263 assertions green with every frozen zMicro/salt/lifetime/anchors golden intact (tier2 consumes tier1 records). Fail-closed matrix extended — negative/float/over-bound `incMs` (and a negative `incMs` into `tier1Record`) throw Tier1InputError. The cross-lane synthetic corpora (gen-cheater-corpus `clockMs {w:0,b:0}`, accounts-fixture parity `Asbla6XbjafLCwGgZgIx8hS4e5MW_E_tmQYhqOqEyko`, calibration) call `tier1Record` with no `incMs`, so their digests are unaffected by construction — not re-run per the no-engine directive. Suites green: test-accounts-tier1.mjs (220 assertions, +16 from A5-15), test-accounts-tier2.mjs (263), npm run typecheck (all three tsconfigs).

**Residual.** The derivation is now correct but DORMANT until the production wiring lands: the A6/embedder (and any verdict-path caller) must thread the witness-signed `RatedBinding.tc.incMs` from the segment into `tier1Record` — until then real games default to `incMs=0` and still net the pre-fix delta. That wiring is out of this lane and is exactly documented-accepted (a) — clockFit is uncalibrated and NOT a Tier-2 z input today (tier2 reads only acpl/match), so no verdict is wrong now; A5-15 removes the structural-aliasing blocker so the wiring is a pure pass-through with no further Tier-1 math. BYO-YOMI is not correctable here and is a separate cross-lane item: `periodMs` rides no signed `RatedBinding.tc` field and the period reset (`afterMoveCredit`) zeroes the delta, so byo-yomi think-time is unrecoverable from the current signed data — recovering it needs a `RatedBinding`/events schema extension (sign the running pre-move clock or the elapsed) that drifts those signatures and is a design decision. The clock inputs' own provenance (self/opponent-asserted clocks) is the distinct finding A5-16, untouched here; and clockFit's promotion into T/z still awaits the J6 calibration pass — which now has a non-degenerate honest signal (no longer 0-aliased) to fit.


### A5-16 — [FIXED] Clock forensic consumes unvalidated clock inputs no honest party checks: the accused's own self-asserted clockMs (`after`) AND the opponent-signed `before` snapshot — either side can drive clockFitMicro to an extreme

- **File:** `src/shared/accounts/judge/tier1.ts`  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** `clockSamplesForSide` now derives the accused's think-time `before` from **s's OWN previous self-signed remaining clock** — the snapshot from its move at ply i−2 (same mover parity) — instead of the opponent-signed ply i−1 echo (`before = prevOwn >= 0 ? moves[ply−2].clockMs[side] : moves[ply−1].clockMs[side]`). This ports into Tier-1 the discipline the witness already enforces for its own flag logic (`witnessCore.ts` ownClock / "finding A": *"time the to-move side against ITS OWN value only, never the opponent's echo … trusting the opponent's echo let a mover zero the honest player's clock and force a witnessed false-flag against them"*). A mover controls BOTH clock fields it signs, so the pre-fix ply i−1 echo let the OPPONENT zero every `before` → all thinks 0 → T=0 → the hardcoded maximal-suspicion `clockFitMicro=0` against an honest accused (the §0 no-adversary-asserted-input lesson, inside the digest-bound canonical `Tier1Record`). Because a side's remaining clock is frozen during the opponent's move, `clockMs[i−2][s] = clockMs[i−1][s]` for every honest transcript, so the change is **byte-identical for every valid record** and alters only adversarial (framing) transcripts. The **sampling geometry is unchanged** (`ply ≥ 1`, same `moverOf` filter ⇒ identical `clockN`), so no honest record's shape shifts; the only opponent-echo-sourced sample that remains is the second mover's opening reply (ply 1), which has no own prior snapshot and falls back to the ply−0 value — a single bounded sample that cannot drive the T-sum to an extreme. No `PARAMS_A5` change (no `PARAMS_A5_DIGEST` drift), no anchors/corpus/golden edits. The `tier1.ts` header CLOCK-FORENSICS block and the `clockSamplesForSide` docstring now document what is enforced (the opponent can no longer influence the accused's forensic) versus the residual (below).

**Verified.** New deterministic regression in `scripts/test-accounts-tier1.mjs` §(d3) (synthetic fixtures, no engine) reproduces the finding's exact framing scenario and proves it is defeated: a 20-ply game, honest White (first mover) spending think ∝ complexity (1800ms hard / 360ms easy → proportional), Black signing `clockMs.w = 1` on every odd ply (zeroing its echo of White's clock) → White think times `[360,1800,360,1800,360,1800,360,1800,360]` are **UNCHANGED** honest vs framed (own ply−2 delta), `w.clockFitMicro` STILL `1_000_000` (was driven to `0` pre-fix), `clockN` still 9, and the honest and framed `Tier1Record`s are **byte-identical** (equal `tier1Digest` — differed pre-fix); the pre-fix collapse is pinned as such (the tampered echo `framed[1].clockMs.w = 1`, and the ply-2 opponent-echo delta `1 − 597840 ≤ 0` would clamp to `0`). The bounded ply-1 residual is proven directly: tampering ONLY the ply−0 echo (`moves[0].clockMs.b = 1`) zeroes exactly the Black ply-1 sample and leaves every later Black sample identical (own snapshots), with `clockForensicMicro` staying `> 0` — a single adversarial value cannot force the maximal-suspicion extreme. Honest play is provably unchanged: the pre-existing frozen `FIX_C` `tier1Digest` (`chzY1umBfAfE9M6Ce6u_73xtvCaMEIwOK7dw_mKVm-8`) and the `644445` bot-white / `1e6` human-black clock goldens all pass byte-identical, and the A5-15 increment goldens are untouched. Suites green: `test-accounts-tier1.mjs` (232 asserts, +12 over the pre-fix 220), `test-accounts-tier2.mjs` (263, all frozen goldens intact), `npm run typecheck` (all three tsconfigs), and — extra barrel smoke — `test-judge-calibration.mjs` CI=1 non-engine sections (55; the §7 engine receipt SKIPs under CI and re-judges with the live engine, so it was not run — but its receipt moves are `clockMs {w:0,b:0}`, giving zero deltas at either index with `clockN` unchanged, so the corpus `Tier1Record` digests are byte-unaffected by construction).

**Residual.** The **whitewash / evasion direction is NOT closed here** and is out of this lane: with `before` now also the accused's own snapshot, both endpoints are self-signed, so an engine-assisted mover faking complexity-proportional own-clocks still scores a high `clockFitMicro`. Distinguishing a real think from a fabricated own-clock schedule requires the witness's independent `wclk/wts` elapsed timestamps (`witnessCore` emits them; the signed `SignedMove` carries only clocks, not the witness clock stream) — Tier-1 does not yet consume them, and bounding `t_i` against `wclk` would need a segment-schema/caller-threading change plus a new `tier1` input (cross-lane), so it remains the witness / transcript-verification layer's duty. The second mover's ply-1 opening reply keeps its single ply−0-echo sample because no own prior snapshot exists (sourcing the initial time would require threading the signed `tc` starting clock); it is bounded and cannot dominate the statistic. Documented-accepted (a) still bounds current impact: `clockFitMicro`/`clockN` have zero consumers outside tests (not a Tier-2 z input — `tier2`, `mm/trust`, ratings read nothing), so no live verdict/trust/ban is affected today — this fix hardens input provenance (the framing/false-fraud direction the §0 prime directive forbids) before `clockFit` can ever be calibrated into Tier-2.


### A5-18 — [FIXED] Window salt is not a unique function of (root, ladder, window, witness set): any threshold subset of grants is 'valid', so the reveal publisher grinds the boundary post-hoc and honest auditors with different reveals disagree

- **File:** `src/shared/accounts/judge/tier2.ts`  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** `countedGrants` now PINS the salt to a canonical subset when the canonical `witnessSet` is supplied: the `threshold` (= `max(1, min(tLease, |witnessSet|))`) smallest-NodeId members of the witness set, in fixed `compareKeys` order (the FULL CANONICAL THRESHOLD SET). The reveal MUST carry a valid grant from EVERY one of them — a missing one yields the deterministic sorted error `salt: canonical grantor <w> did not sign — the pinned threshold subset is incomplete`, so `verifySaltReveal` rejects and `windowSalt`/`saltRevealBytes` throw. Grants from the remaining larger-NodeId witnesses are legal availability redundancy but NEVER enter the salt, so the salt is INVARIANT to which supra-threshold subset the reveal carries. Hence `windowSalt` is a UNIQUE function of `(root, ladder, window, witnessSet)`: the reveal assembler can no longer choose the subset to slide `off(w)`/`b(w)` post-hoc (§7b anti-surf restored), and two honest auditors holding DIFFERENT but both-valid reveals derive the SAME salt → the SAME closed-window partition → the same `z_w`/J7 `lifetimeVerdict` (the A4-04 consensus-split class closed). The change is a single point — `saltRevealBytes`/`windowSalt`/`verifySaltReveal` consume the returned pinned subset unchanged. WITHOUT a `witnessSet` the canonical set is unknown, so the salt stays reveal-defined (all counted grants), now documented (header + `SaltVerifyOpts.witnessSet`) as a NOT-grind-proof diagnostic/legacy path the consensus/verdict path must not use — canonicalizing a subset of an unknown set is impossible. No `PARAMS_A5` edit (no digest drift), no anchors/corpus/golden change: the no-witnessSet path is byte-identical, so every frozen salt golden holds un-refrozen. `fold.ts` (no salt logic) untouched.

**Verified.** New deterministic regressions in `scripts/test-accounts-tier2.mjs` §1a (synthetic 6-witness canonical set, threshold 4, no engine): the designated-only reveal and ALL supra-threshold supersets (designated+one-extra, all N, reordered) yield the IDENTICAL salt (extra grants never enter it); two honest auditors holding different valid reveals derive the SAME salt → the SAME `saltOffset`/window boundary (cross-auditor partition split closed); a ≥threshold subset OMITTING a designated (small-NodeId) grantor is REFUSED with the typed pinned-subset error and `windowSalt` throws; strict out-of-set rejection preserved; and the CONTRAST — WITHOUT `witnessSet` the very same two subsets produce DIFFERENT salts (the grind the witnessSet path closes). Suites green: test-accounts-tier2.mjs (274 asserts, was 263; all pre-existing salt/offset goldens intact — saltW1/W2/W3, offW1/W2/W3, saltBodyHash, second-bundle determinism), test-accounts-tier1.mjs (268), test-accounts-chain.mjs (164), test-accounts-ratings.mjs (300), npm run typecheck (all three tsconfigs). Grep confirms the salt functions are exercised by no other suite, so no golden elsewhere can shift.

**Residual.** WITHOUT a supplied `witnessSet` the salt is inherently reveal-defined and NOT grind-proof (a subset of an unknown set cannot be canonicalized); the header and `SaltVerifyOpts.witnessSet` doc now state this and direct the consensus/verdict path to supply the canonical set, whose derivation from a directory snapshot stays embedder work (the deliberate storage/pointers.ts thinness — the same accepted class as witness-set membership validation, residual (e)). The separate §7b TIME-binding gap (A5-17: the salt tuple commits to no post-game entropy and `wts` has no floor, so a witness can pre-sign) is orthogonal to subset-uniqueness and untouched here. The `SaltReveal` record is still unsigned/unanchored with no publishing slot — the pinning makes its exact contents non-load-bearing for the salt VALUE (any valid reveal → the same salt), but WHICH reveal is authoritative for publication remains A6 storage plumbing.


### A5-20 — [FIXED] escalationDue returns the trailing-K firing even when the J7 lifetime trigger escalated EARLIER in chain order — the self-ban deadline is set too late, minting false suppression / a consensus split

- **File:** `src/shared/accounts/judge/tier2.ts`  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** escalationDue no longer early-returns on the trailing-K firing before the J7 lifetime arm is ever reached. The two arms are now evaluated INDEPENDENTLY: the trailing-K scan records its earliest firing (`atIndex/game/zMicro`) into a local without returning, the lifetime scan records its earliest escalating closed-window prefix (`lifetime = {zLifeMicro, windows}`), `due` is their OR, and when BOTH fire BOTH are surfaced in the EscalationVerdict — previously a trailing-K firing returned `{due, atIndex, game, zMicro}` with `lifetime` dropped, silently discarding a lifetime escalation that (over closed salted windows) can cross the line at an EARLIER chain ordinal. The §8 self-ban deadline anchors on whichever fired FIRST — `games[atIndex]` for trailing-K vs the game closing window W−1 (ordinal b(W)−1) for lifetime — and, by the module's standing design, only the partition-holding caller can name b(W)−1, so it takes the min (the same window→ordinal resolution the `lifetime` field's contract already assigns it). escalationDue itself never sees the salted partition and so cannot order the two; reporting both LOSSLESSLY is precisely what lets the caller set the deadline no later than the earliest trigger (returning trailing-K alone minted a too-late deadline / a provable-suppressor split when lifetime fired earlier). The `EscalationVerdict.lifetime`/`atIndex` and escalationDue doc blocks were rewritten to state the both-may-be-present / caller-mins-by-ordinal contract. No PARAMS_A5 change (no digest drift); the 3-arg path (no `closedWindowZs`) skips the lifetime arm entirely and is byte-identical to legacy, so every frozen 3-arg golden holds. No anchors/corpus/golden changes.

**Verified.** New deterministic regressions in scripts/test-accounts-tier2.mjs (synthetic Tier1Records, no engine) replacing the assertion that PINNED the buggy precedence ("4-arg result identical when the window trigger already fired"): on the 25-innocent→blatant fixture that fires trailing-K at index 31 (z = 3,411,623 = 3.41σ), `escalationDue(games, records, A, [met26, met26])` now returns the trailing-K anchor (atIndex 31, game blat-31, zMicro 3,411,623) AND the lifetime firing (windows 2, zLifeMicro 3,677,510) — the earlier firing is no longer discarded — and the both-arms result is bit-deterministic across repeated calls; the finding's exact W=1 shape `escalationDue(games, records, A, [3_000_000])` now surfaces `lifetime {windows:1, zLifeMicro:3_000_000}` alongside atIndex 31 (pre-fix it returned atIndex:31, lifetime:undefined); the 3-arg call still carries no `lifetime` key (byte-unchanged legacy path). The pre-existing lifetime-only firing (innoGames + [met26,met26,9M] ⇒ windows 2, atIndex undefined), the honest-cancellation {due:false}, and all trailing-K / under-K goldens are intact. Suites green: test-accounts-tier2.mjs (285 asserts), test-accounts-tier1.mjs (268), npm run typecheck (node/web/server).

**Residual.** The caller-side min is unexercised in-lane because no production deadline-anchoring consumer exists yet — verdict publish / suppression scan / `selfBanExpiryWts` wiring is A6 embedder work, and the sole 4-arg caller today (scripts/test-judge-calibration.mjs, out of lane) only asserts `.due` and is logically unaffected. escalationDue now hands that future consumer both firing positions; anchoring the §8 deadline on `min(games[atIndex].wts, games[b(W)−1].wts)` — mapping the lifetime window W to its salted-partition ordinal b(W)−1 — remains the partition-holding caller's documented duty, now UNBLOCKED (before, the earlier firing was unrecoverable). The prefix-vs-one-shot evaluation split noted by the verifier is inherent to any growing-chain trailing trigger and is not a return-shape defect. *[Superseded 2026-07-22: A5-21 re-anchored the BAN deadline on the `conviction` report (same lossless both-arms / min-by-ordinal contract this fix established, applied at the 5σ line); the escalation firings this entry surfaces now anchor only the deeper-analysis obligation.]*


### A5-21 — [FIXED] Self-ban / suppression obligation is gated on the 3σ escalation trigger, not the 5σ conviction — ban FPR is ~1.3e-3/window, not astronomically low

- **File:** `src/shared/accounts/judge/tier2.ts` (`selfBanDueNow` / `escalationDue` / suppression gates)  ·  **Severity:** major  ·  **Status:** DECIDED + FIXED 2026-07-22

**DECISION (owner-delegated, 2026-07-22).** The owner directive — *"I don't want honest players getting banned, ever"* — resolves §8's internal ambiguity exactly as this review recommended: the self-ban / suppression obligation is RE-ANCHORED on the 5σ CONVICTION line (`zThresholdMicro`); the 3σ escalation keeps only its deeper-analysis role and can never oblige a ban or ground a suppression. Rationale: at 3σ the one-sided ban FPR ≈1.35e-3/window makes an honest career's eventual false 90-day self-ban (or, if unaware, permanent distrust) near-certain — 22.9% / 57.9% / 93.5% over 1k / 3k / 10k games — the §0 false-fraud channel; at 5σ the per-look FPR is ≈2.9e-7, union-bounded < 3e-3 over 10⁴ windows (§8's own multiplicity budget). Sustained metering still convicts via the J7 lifetime arm (W=4 at 2.6σ/window), so no detection capability §8 mandates is lost — only the false bans. `docs/building/ACCOUNTS-SPEC.md` §8 was amended in place with the decision note (conviction-anchored obligation + deadline, "never relative to mere escalation").

**Fix.** `escalationDue` now scans each arm for BOTH lines and reports an A5-21 `conviction` field alongside the escalation firings: `conviction.atIndex/game/zMicro` = the EARLIEST trailing-K window whose recomputed z ≥ `zThresholdMicro` (the scan no longer breaks at the escalation crossing), `conviction.lifetime` = the EARLIEST convicting closed-window prefix (`lifetimeVerdict(...).convicted`), with the same A5-20 lossless both-arms / caller-mins-by-ordinal contract applied at the conviction line — the §8 self-ban deadline anchors on the earliest CONVICTION firing, never an escalation firing. Structurally conviction ⇒ escalation, so `conviction` never appears on a not-due verdict and `due` (the deeper-analysis obligation) is byte-unchanged. `selfBanDueNow` gates on `escalation.conviction` (object-checked fail-closed), not `due`. `makeTier2Verdict` + `verifyTier2Verdict` suppression gates move from `zEscalateMicro` to the conviction: a full-reganK window with RECOMPUTED zMicro ≥ `zThresholdMicro`, or a lifetime claim whose RECOMPUTED `zLifeMicro` convicts — the A5-03 derivable-from-inputs discipline preserved verbatim at the new line, deterministic error `'verdict: suppression conviction did not fire on the recomputed evidence (…)'`, builder throwing symmetrically. Doc blocks rewritten to the decided semantics (module header §trigger + §records, `EscalationVerdict`, `Tier2VerdictBody`, `selfBanDueNow`). NO PARAMS_A5 change (both thresholds pre-existed — no digest drift), no anchors/corpus change, no source golden re-freeze; the only new goldens are suite literals for the conviction crossing of the existing 25-inno→blat fixture (`convAtIndex` 34, `convZ` 5_109_979) and reuse of the existing `zLife4x26` 5_200_000.

**Verified.** Re-cut scripts/test-accounts-tier2.mjs (synthetic fixtures, no engine), ALL GREEN — 373 assertions (was 343): the canonical suppression fixtures now CONVICT (window z 5_109_979 at index 34; lifetime W=4 zLife 5_200_000 metering); the PRE-DECISION VALID fixture — the 3.41σ escalation-band full-K window — is REFUSED by both mint paths (hand-forged signed record rejected with the typed conviction error; builder throws), as is an ESCALATING-but-sub-conviction lifetime claim (3.67σ, W=2); `selfBanDueNow` is FALSE across the entire [3σ,5σ) band — the escalation-band chain (max z 4.54σ), a single +3σ-capped game (W=1 lifetime — the "worse than filed" single-game ban is dead), and the top-of-band statistic (`zThresholdMicro − 1`) — and TRUE at the trailing-K conviction (deadline anchor game `blat-34`), at the lifetime conviction (metering W=4), and exactly AT `zThresholdMicro` (inclusive boundary, obligation ≡ conviction). A5-26's threshold-boundary matrix is retained; its two obligation-level assertions flipped deliberately with this block (as its own annotation required). escalationDue conviction reporting pinned: earliest convicting index 34 ≠ earliest escalating index 31; the [met26,met26,9e6] fixture reports escalation W=2 AND conviction W=3 (zLife 8_198_614) independently; all-innocent chains carry no conviction. Suites green: test-accounts-tier2.mjs 373, test-accounts-tier1.mjs 268, test-accounts-ratings.mjs 300, test-accounts-chain.mjs 164, CI=1 test-judge-calibration.mjs 73 (its escalationDue consumers read only `due`/`atIndex`/`lifetime` — byte-compatible), npm run typecheck (node/web/server).

**Post-fix adversarial review (2026-07-22).** Fable-pinned workflow (5 attack angles → independent technical refuter + relevance judge per finding; 35 agents, per-agent model audit clean — 100% `claude-fable-5`): 15 candidates → 11 confirmed (4 substantive + 7 stale-language) → ALL FIXED same day. Substantive: (a) the extended conviction scan had silently widened escalationDue's deep-validated input domain scan-depth-dependently (malformed records/window-zs beyond the first escalation crossing flipped verdict→throw vs the pre-change evaluator) — RATIFIED as upfront full-domain validation (every entry + every closedWindowZs element validated before any window is evaluated; throw behavior is now input-shape-determined, strictly fail-closed, declared in the doc block) with regressions for all three malformed-domain classes; (b) the inclusive ≥ at zThresholdMicro was unpinned on all three new comparisons (an exclusive-comparator mutant passed the whole suite) — now pinned by a crafted 30-game window at z = EXACTLY 5_000_000 (10 capped-blat + 20 fills at dev −130_750) minting + verifying a suppression and convicting in escalationDue, with its one-micro-below twin (4_999_999) escalating but refused everywhere; (c) the conviction report's lossless both-arms / earliest-prefix contract was untested (drop-other-arm and report-last-prefix mutants survived) — now pinned (both arms convict simultaneously; earliest convicting lifetime prefix W=4 stable under 6 supplied windows). Stale-language: 'trigger'-anchored ban wording corrected in types.ts SelfBanPayload, selfBanExpiryWts (param renamed convictionWts), fold.ts banStep comment, the module's architectural rule (b), the suite header + A5-26 preamble, and supersession notes added to the A5-03/A5-07/A5-20/A5-26 entries above. Post-fix gates: test-accounts-tier2.mjs 394 assertions ALL GREEN (373 → 394), tier1 268, ratings 300, chain 164, CI calibration 73, typecheck (node/web/server) exit 0.

**Residual.** (1) The caller-side deadline min at the conviction line remains A6 embedder work — the same class as A5-20's residual, now evaluated at the conviction: `escalationDue` surfaces both conviction firings losslessly; the partition-holding caller maps a lifetime conviction's window W to ordinal b(W)−1 and anchors the §8 deadline on `min` by chain ordinal. (2) `fold.ts` banStep / displayState / pairing are deliberately untouched: they fold and surface self-ban EVENTS; the obligation semantics live at this layer. (3) The A5-02 RD-aware scoring and A5-09 longitudinal holdout compose with this fix; A5-09's other mode (a settled account's within-band style residual) still awaits the pre-ship recalibration, but its ban-FPR exposure is now bounded by the 5σ line instead of 3σ. (4) types.ts `SelfBanPayload` doc ("window names the K-window that convicted") is now literally true. (5) The pre-decision analysis this entry replaces (the §8-ambiguity reading and the 3σ-vs-5σ tradeoff) is condensed into the DECISION paragraph above; the suite's pre-decision characterization block was deliberately replaced by the re-anchored regressions, exactly as its own annotation required.


### A5-22 — [FIXED] Self-ban expiry is self-asserted and non-monotonic — a convicted account un-bans itself with a second selfban

- **File:** `src/shared/accounts/ratings/fold.ts` (banStep)  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** `banStep` no longer copies `payload.expiryWts` under a "newest wins" overwrite. The §9 ban term is now DERIVED, never self-asserted: `until = ev.body.ts + PARAMS_A5.selfBanDays·MS_PER_DAY` — `ev.body.ts` is the selfban event's diversity-bound §4 witnessed time (witness-clock-checked in attest.ts:158-159, `Math.abs(wts − b.ts) > timeWindowMs ⇒ refuse`) and is ≥ the §8 trigger, since the compliant client appends the selfban immediately after the K-window-completing game — so `until ≥ triggerWts + 90d`, never under-banning. And it folds MONOTONICALLY: the ladder entry is replaced only when the new derived `until` is strictly greater (max, first-wins on tie), so a later selfban carrying a smaller/zero/earlier expiry is a no-op — the un-ban attack is dead, and `{until, window, verdict}` always name one convicting selfban. `PARAMS_A5` is imported from the pure-data `../judge/params` (codec+hash only — no cycle, fold stays platform-neutral); `MS_PER_DAY` is a LOCAL unit const mirroring witness/pin.ts and judge/tier2.ts (a physical constant, not a tunable — no PARAMS_A5_DIGEST drift, `judge/params.ts` untouched). Consumers are unchanged (displayState/pairing.ts read only `until`). No events.ts/chain.ts edit (out of lane) and none is needed: the fold is the state-deriver feeding display/pairing, so a derived+monotonic fold renders verifyChain admitting two selfbans harmless. Doc blocks updated (bans state, LadderBan.until, banStep) to state "derived + monotonic, never the self-asserted expiryWts".

**Verified.** New deterministic regression in scripts/test-accounts-tier2.mjs §7 (synthetic events, no engine) reproducing the finding end-to-end (fold → displayState → pairingLegal): a compliant selfban (event ts = trigger, payload expiry = selfBanExpiryWts(trigger)) then a SECOND `{window:0, expiryWts:0}` selfban at trigger+1day (mid-sentence). Post-fix all green — `until` extends to ≥ compliantExpiry (1_807_776_000_000), displayState stays `banned`, pairingLegal stays illegal `banned`; a pure monotonic-guard case (a selfban with an EARLIER ts / smaller derived until) leaves the ban and its window/verdict unchanged. CONTROL: temporarily reverting `banStep` to `until := p.expiryWts` turns the suite RED (exit 1) on exactly these assertions — `bans[LAD].until` = 0, displayState = `ranked`, pairingLegal legal (the convict resumes rated play — the finding's scenario) — then restored byte-for-byte and re-run green. Existing §7 goldens re-frozen IN THIS FILE: the two `until` assertions now expect the derived `untilOf = m.selfBanExpiryWts(eventTs)` (7_776_001_001 / 7_776_001_002 at ts 1_001/1_002) instead of the self-asserted 9_000_000 / 12_000_000, plus a new assert that the payload's expiryWts is IGNORED. Suites green: test-accounts-tier2.mjs (293), test-accounts-ratings.mjs (300 — fold bytes identical on selfban-free chains: `banStep` still returns the same reference for non-selfban events), test-accounts-chain.mjs (164), npm run typecheck (node/web/server). The only in-repo selfban-folding suite is test-accounts-tier2 (my lane), so all changed state bytes are re-frozen here.

**Residual.** The fold still cannot recompute the EXACT §8 `triggerWts` (the K-window-completing game's witnessed time) — that requires the judge engine, which no synchronous fold step runs (the lead-decided architecture; the same reason suppression is a read-time record). Anchoring the term on the selfban EVENT's witnessed ts (≥ triggerWts) enforces `until ≥ triggerWts + 90d` monotonically and kills every self-un-ban / self-asserted-expiry vector (including a lone `{expiryWts:0}` selfban, which now still imposes a real 90-day term from its witnessed time), but the millisecond-exact deadline equality and the "a selfban was actually OBLIGATED at this point" check remain the read-time verifier's duties (suppression record, tier2.ts), unchanged. zSelfBanPayload (events.ts:231) and chain.ts still admit two selfbans per ladder and an unconstrained payload `expiryWts` — untouched (out of lane) and now harmless (the fold ignores `expiryWts`); tightening the schema/chain and dropping the now-advisory `expiryWts` field are optional defense-in-depth for the events.ts/chain.ts owner. The shrink-direction regression A5-08 called for now exists in this file; A5-08's separate chain-suite duplicate-selfban-admission coverage stays its own scope.


### A5-23 — [FIXED] Web adapter: close()/killed worker never notifies onError and send() silently no-ops, so an in-flight or post-close judgeGame hangs forever (node counterpart fails closed)

- **File:** `src/web/engines/judge.ts:87`  ·  **Severity:** major  ·  **Status:** FIXED 2026-07-22

**Fix.** The web adapter's engine object is now built by an exported `makeWorkerJudgeEngine(worker, wasmBlobUrl)` (newWebJudgeEngine delegates to it — identical fetch+hash gate, worker spawn, uci/isready handshake, blob-URL revoke and return surface), and its teardown is FAIL-CLOSED like the node adapter (server/judge/nodeAdapter.ts). A single `teardown(err)` runs on EITHER an explicit `close()` OR a worker `error` event: it posts `quit`, terminates the worker, revokes the verified-bytes blob URL, and — because Worker.terminate() fires no `error` event — notifies every onError subscriber exactly once, guarded by `closed` so crash-then-close or close-then-crash still fails once (mirrors node's `if (exited) return` in quit()). judgeGame's onFatal wiring turns that notification into a JudgeEngineError, so an in-flight barrier/analyseOne REJECTS instead of awaiting a `readyok`/`bestmove` the dead worker can never send. And `send()` after teardown now THROWS `judge engine worker is closed` instead of silently no-op'ing, so a judgeGame begun on a closed/dead engine rejects at its first `engine.send('stop')` — the exact fail-closed shape of the node path (nodeEngine.ts:203-206 send() throws 'judge engine has exited'; quit()->child exit->onExit->onError->JudgeEngineError, nodeAdapter.ts:37-38). This honors onError's documented contract (types.ts:101-105 — it exists "so an in-flight judgeGame fails instead of hanging"). No PARAMS_A5/params.ts edit, no digest drift, no golden re-freeze.

**Verified.** New engine-free regression in scripts/test-judge-node.mjs §'J1 web adapter A5-23' (placed before any engine spawns, so cheap + CI-safe) drives the REAL judgeGame() over a spec-faithful fake Worker — terminate() fires NO events per the HTML spec; it auto-answers the isready barrier with readyok and on `go` either replies MultiPV info+bestmove or stays silent to model a search still in flight: (0) a full judgeGame COMPLETES and a post-completion close() terminates cleanly (harness faithful, happy path untouched); (1) close() while position 0's search is in flight -> judgeGame REJECTS with JudgeEngineError, no hang; (2) judgeGame on an already-closed engine REJECTS via the send() throw (message /closed/, not TIMEOUT); (3) a worker `error` event REJECTS the in-flight judgeGame AND a post-death send() throws. A REF'd hang-guard (withTimeout) converts any regression to a loud TIMEOUT ✗ rather than a stalled suite; teeth confirmed out-of-band by running the same four scenarios against a transcribed pre-fix engine object (silent-no-op send, close without errCb notify) — its in-flight and post-close paths never settle. Suites green: test-judge-node.mjs (58 assertions, was 50; frozen Tier-1 digest `F7XHAGNBkIR_2l6H7J8-Y2JTOOFj5SBtyYGvPAFIl3s` reproduced bit-for-bit — the refactor changed no verdict bits), test-judge-fit.mjs (66), npm run typecheck (all three tsconfigs).

**Residual.** The CONFIRMED defect is fully closed: explicit close() AND a worker `error` event now both fail-closed, and post-death send() throws. The one sub-scenario not lab-verifiable here — a browser silently reclaiming the worker under mobile memory pressure WITHOUT firing an ErrorEvent (§8/§11 name phones as first-class judge platforms) — stays a latent liveness gap: with no error/exit signal nothing triggers teardown, and judgeGame still has no per-position watchdog (only the pre-judgeGame handshake carries JUDGE_START_TIMEOUT_MS). A judgeGame-level watchdog needs a calibrated per-position node->wall-time budget (a PARAMS_A5 tunable that would drift PARAMS_A5_DIGEST) and is engine/calibration work outside this surgical budget — deferred to A6 browser wiring. All current in-repo callers close only after judging (accounts-fixture JUDGE_ENGINE_ENTRY_TS), so it is latent today; the real-browser engine-parity gate (test-web-accounts-browser.mjs) is CI-skipped and outside this lane, so the fix is proven via the node-hosted fake-Worker regression rather than a live browser run.


## Minor (14)

### A5-11 — [FIXED] 'Tampered wasm refused' test only proves the wasmPath ARGUMENT is hashed — the §8 invariant 'verified bytes are the executed bytes' has no test and a divergent checked/loaded pair passes every suite

- **File:** `scripts/test-judge-node.mjs`  ·  **Severity:** minor  ·  **Status:** FIXED 2026-07-22

**Fix.** A5-11 is the test-coverage twin of the A5-13 gate fix (loadedWasmPath/gatedWasmPath/JudgeWasmPathError — the §8 gate now hashes the EXACT `__dirname` sibling the spawned `node <glue>` child self-loads, never a decoupled wasmPath). test-judge-node.mjs gains a dedicated engine-free section, "J1 content-hash gate A5-11: verified bytes ARE executed bytes", that constructs the finding's exact divergent pair — a glue COPY whose own sibling (loadedWasmPath) is byte-flipped, gated by the PRISTINE node_modules wasmPath — and pins the COUNTERFACTUAL the A5-13 refusal matrix leaves implicit, so the checked/loaded split is proven REAL, not tautological. It asserts (i) `loadedWasmPath(glueCopy)` is the sibling the child self-resolves; (ii) the pristine wasmPath ARGUMENT verifies CLEAN (all the pre-fix argument-only gate ever hashed → green); (iii) that child-loaded sibling is UN-PINNED (verified ≠ executed by construction); (iv) `newNodeJudgeEngine` on the clean-argument/tampered-sibling pair is REFUSED before spawn with a typed JudgeWasmPathError (a clean decoy cannot redirect the pin); and (v) with NO argument the gate hashes the EXECUTED sibling itself and catches the tamper as JudgeWasmHashError — the direct statement that verified bytes are the executed bytes. Its own fixtures (`a5_11-glue.js`/`.wasm`) keep it independent of the adjacent A5-13 block, and both throws precede any child, so the section is fully engine-free. No PARAMS_A5/params.ts edit, no digest drift, no golden re-freeze.

**Verified.** The new section is 5 green assertions with no engine (both refusals throw before any process spawns). An independent engine-free probe (esbuild-bundled adapter/engine/content/core, no spawn) confirms the regression BITES: the executed sibling reports `verifyWasmHash ok=false` while the pristine argument reports `ok=true`, the current gate throws JudgeWasmPathError (divergent pair) and JudgeWasmHashError (no-argument tampered sibling), and a SIMULATED pre-fix gate that hashes only `opts.wasmPath` PASSES the divergent pair — it would spawn the tampered engine on a green check, exactly the "ALL GREEN while minting un-pinned verdict bits" the finding describes, so the try-block's `ok(false, …ACCEPTED…)` fires red pre-fix. The frozen Tier-1 digest `F7XHAGNBkIR_2l6H7J8-Y2JTOOFj5SBtyYGvPAFIl3s` still reproduces bit-for-bit after the new section. Suites green: test-judge-node.mjs (63 asserts, was 58; +5), test-judge-fit.mjs (68, in-lane, unchanged), npm run typecheck (all three tsconfigs).

**Residual.** The section proves the gate's TARGET equals the child's self-loaded sibling and that a tampered/divergent target is refused, all via loadedWasmPath's pure path computation — it does NOT spawn a child to prove the shipped Emscripten glue REALLY `readFileSync`'s `join(dirname, basename+'.wasm')` at runtime (an engine spawn, out of this deterministic budget); the A5-13 code makes the gate hash exactly that computed sibling, and the suite's existing loaded-sibling happy-path assert fails loudly if a glue upgrade ever moved its wasm resolution. The web adapter's structural analogue (instantiate from the verified-bytes `blob:` URL, src/web/engines/judge.ts) still has only fail-closed-teardown coverage (the A5-23 fake-Worker section), not a byte-level "instantiated == verified" assertion — that needs a Worker/DOM harness and is separate web coverage.


### A5-17 — [DEFERRED] Window salt commits to no post-game entropy and no time binding, so the frontier is computable BEFORE the games — §7b 'unpredictable before' rests entirely on unenforced witness signing-time discipline

- **File:** `src/shared/accounts/judge/tier2.ts`  ·  **Severity:** minor (searcher filed major; verifier adjusted)  ·  **Status:** DEFERRED — **RATIFIED 2026-07-22** (owner-delegated): the in-lane anchor mechanism + requireAnchor gate landed; the remaining witness signing-time discipline and embedder anchor-wiring are A6 scope. No honest-ban exposure — the gap affects only pre-computability of the salted window unit (a cheater-evasion surface), and the ban path now additionally requires a 5σ conviction (A5-21).

**Fix (strongest in-lane binding landed).** `saltBodyHash` / `signSaltGrant` / `SaltReveal` / `zSaltReveal` now accept an OPTIONAL post-game `anchor` (a 32-byte b64u commitment) folded into the signed salt body, so `windowSalt(w)` — hence `off(w)` and the whole partition — commits to chain state fixed only AFTER the games preceding boundary b(w) are played (the embedder binds the rated-game key at ordinal w·K−1: the LATEST ordinal guaranteed < b(w) for every off ∈ [0,K), so non-circular and recomputable-after, or a digest over [0, w·K)). Because the anchor enters `grantBytes`, a witness's message for window w LITERALLY DOES NOT EXIST until that game is chained — an honest witness (signing only over chain state it has observed) cannot form or pre-sign it — and any post-hoc anchor swap invalidates every grant (`verifyGrantSig` fails: unforgeable). A new `SaltVerifyOpts.requireAnchor` makes the consensus/verdict path REJECT any anchorless (predictable-before) reveal with a deterministic §7b error (and `windowSalt`/`saltRevealBytes` throw under it); it composes orthogonally with the A5-18 `witnessSet` pin (grind-proof AND unpredictable-before). Absent an anchor the derivation is byte-identical to the pre-A5-17 legacy path, so every existing salt golden holds un-refrozen. No PARAMS_A5 change (no digest drift). The module header's overclaimed "unpredictable before the witnesses sign" is corrected to the honest post-game / signing-time framing, and the boundary-jitter claim now cites the anchor + its deferred residual. A time-only alternative (a `wts` floor with no post-game commitment) was considered and rejected as strictly weaker (self-asserted time, no chain binding).

**Verified.** New deterministic regressions in scripts/test-accounts-tier2.mjs §1b (synthetic grants, no engine): the anchored salt body/salt differ from the anchorless golden while the anchorless body stays byte-identical to `GOLDEN.saltBodyHashW1` (legacy path unchanged); a malformed anchor is refused; recomputable-after — same anchor ⇒ bit-identical salt (new golden `saltW1A` pinned 2026-07-22); a DIFFERENT post-game anchor ⇒ a different salt AND a different `off(1)` (the K-window frontier MOVES with post-game chain state, not a line fixed at account creation); a post-hoc anchor swap ⇒ reveal rejected; `requireAnchor` REFUSES an anchorless reveal with the deterministic §7b error and `windowSalt` throws under it, while an anchored reveal verifies and composes with the `witnessSet` pin. Suites green: test-accounts-tier2.mjs (316 asserts, all pre-existing salt/verdict goldens intact), test-accounts-tier1.mjs (268), test-accounts-ratings.mjs (300), test-accounts-chain.mjs (164), npm run typecheck (node/web/server). Engine-heavy calibration + browser suites deliberately not re-run — the no-anchor path is byte-identical, so their goldens are unaffected.

**Residual (DEFERRED — the exact requirement).** FULL §7b soundness needs two cross-lane pieces this platform-neutral core cannot enforce: (1) WITNESS-NODE SIGNING-TIME DISCIPLINE — a canonical witness MUST refuse to sign window w's salt grant until it has observed the anchor game (ordinal w·K−1) on-chain, and set `wts` to the window-close diversity-bound witnessed time (§4); without it a malicious/colluding witness can still pre-sign, though now only over a chain anchor it has NOT verified (a narrower, attributable fault than signing a t=0-static tuple). (2) EMBEDDER ANCHOR-WIRING — the consensus/verdict path must derive `anchor` from the chain-ordered rated-game list (game key at ordinal w·K−1, or a digest over [0, w·K)) and pass `requireAnchor: true`; no such caller exists yet — the salt→partition→verdict path is not yet chain-wired (the same deferred embedder duty as the A5-18 directory-snapshot `witnessSet`). Until both land, the DEFAULT in-tree derivation remains predictable-before (diagnostic/legacy) and runtime anticheat behavior is unchanged; the in-lane mechanism + enforcement gate are ready for the embedder to invoke. Note (bounding, per the verifier): the primary self-ban-obliging trigger is the salt-free trailing-K fixed geometry, so this gap targets only the salted per-window verdict unit and the J7 lifetime Σz_w/√W, not the main escalation path.


### A5-24 — [FIXED] Operator peer's 'last-resort Tier-2' judge capability is tested only for construction in a non-CI smoke script — its analyseFixedNodes output is never cross-checked against the canonical judgeGame surface

- **File:** `scripts/operator-smoke.mjs:111`  ·  **Severity:** minor  ·  **Status:** FIXED 2026-07-22

**Fix.** Paired with the A5-32 fix (operator peer routed through the canonical judgeGame path): operator-smoke.mjs now exercises the peer's judge END-TO-END rather than only asserting construction — driven over a MULTI-position fixture (JUDGE_PARITY_POSITIONS, TT-granularity-sensitive so the parity check has teeth), bit-cross-checked against the canonical judge surface, config-echo-bound to PARAMS_A5_DIGEST, and judgeGame asserted to fail-close on per-position TT reset before any engine I/O. CI-safe by construction (MockFabric, pinned-WASM footprint already in the CI wall via test-judge-node).

**Verified.** operator-smoke.mjs ALL GREEN — 20 assertions (was 11): the new block asserts the parity fixture is multi-position, then that a receipt computed through the peer handle names the current rule set and bit-matches the canonical surface. Verified from the lead shell.

**Residual.** None. (This finding's partial edit landed on disk before the 2026-07-22 workflow auth-failure; completeness confirmed by the green suite and this section reconstructed by the lead from the committed regression.)


### A5-26 — [FIXED] selfBanDueNow tests pin the ban obligation at a 3.41σ escalation and no test anywhere separates escalation consequences from the 5σ conviction line

- **File:** `scripts/test-accounts-tier2.mjs` (§6 self-ban helpers)  ·  **Severity:** minor  ·  **Status:** FIXED 2026-07-22

**Fix.** Added a dedicated A5-26 block to §6 of scripts/test-accounts-tier2.mjs, immediately after the A5-21 characterization block, that AFFIRMATIVELY exercises the module-header property "escalation only obliges deeper analysis, never convict" (tier2.ts NO-SINGLE-GAME-CONVICTS / J7-multiplicity notes) — the coverage the finding names as absent — and locks the two thresholds' DISTINCT consequences against each other. Concretely: (1) a direct assertion that the gates are ordered, `zEscalateMicro < zThresholdMicro` (never collapsed); (2) using the suite's already-golden W=1 lifetime identity (`lifetimeVerdict([z]).zLifeMicro === z`, §3b), four EXACT-boundary assertions astride each gate — one micro-σ below 3σ ⇒ neither flag; AT 3σ ⇒ escalate-not-convict; one micro-σ below 5σ ⇒ STILL escalate-not-convict (the header property, pinned at the top of the band); AT 5σ ⇒ convicted (with conviction ⇒ escalation, never the reverse); (3) consequence separation at the OBLIGATION level — the top-of-band sub-conviction statistic (zThreshold−1) drives `escalationDue.due` and `selfBanDueNow` across the WHOLE [3σ,5σ) band, not only at the single 3.41σ fixture point the suite previously pinned, showing the self-ban obligation tracks the escalation line and is provably distinct from conviction. NO source was touched (tier2.ts/fold.ts unchanged) — pure test additions, so no behavior change and no digest/golden drift. *[Superseded 2026-07-22: A5-21 was DECIDED and the ban re-anchored on the 5σ conviction — the obligation-level assertions in this block flipped deliberately with the A5-21 block (exactly the lockstep this entry mandated), and now pin `selfBanDueNow === false` across the whole [3σ,5σ) band with the obligation firing at the conviction line, including an exact-boundary window at zThresholdMicro. The threshold-ordering and escalate-not-convict boundary matrix this entry added carries over unchanged.]*

**Verified.** The 7 new assertions run GREEN (suite lines 296–302). Deterministic teeth proof (no engine): because the boundary/ordering assertions are pure functions of the two thresholds via the W=1 identity, a regression collapsing the conviction gate onto the escalation line (5σ→3σ) trips 3 of them, and one raising the escalation gate onto conviction (3σ→5σ) trips 3 — the gate-confusion class the suite previously could not catch, now caught. Suites green: test-accounts-tier2.mjs (343 assertions, up 7, all pre-existing goldens intact), test-accounts-ratings.mjs (300, fold.ts consumer), test-accounts-tier1.mjs (268, tier2's tier1 dep), npm run typecheck (all three tsconfigs). No corpus re-judge — the new statistics are synthetic threshold values, not engine outputs.

**Residual.** None for the test-coverage gap. The underlying 3σ-vs-5σ ban-gating POLICY was the separate finding A5-21 — since DECIDED (2026-07-22) and re-anchored on the 5σ conviction; the flip-together annotation this entry installed did its job (both blocks moved as one). The two chain-side suppression claims (that `entries` are the accused's real trailing-K window, and that no selfban precedes the deadline event) remain the auditor's chain-bytes duties by design, unchanged.


### A5-27 — [FIXED] The 'corpus params digest == CURRENT PARAMS_A5_DIGEST' gate is satisfiable by editing a JSONL field — the stamps were already migrated in place after the J7 drift, and the one bit-level receipt is skipped under CI

- **File:** `scripts/test-judge-calibration.mjs`  ·  **Severity:** minor  ·  **Status:** FIXED 2026-07-22

**Fix.** The corpus↔params linkage no longer rests solely on `r.judgeParams == PARAMS_A5_DIGEST` (a rewritable JSONL stamp, migrated in place at the J7 drift) plus a CI-skipped engine receipt. A FROZEN judged-bits golden was added to §1 (corpus integrity): `JUDGED_BITS_GOLDEN = { t1Nodes: 200_000, t1MultiPv: 4, hashMb: 16, scoreEquivCp: 15, ttReset: 'per-game' }` — the ONLY inputs that move the engine's output bits and the score-equivalence match count — asserted field-for-field against LIVE `PARAMS_A5`, INDEPENDENT of any corpus field. Because the check reads PARAMS_A5 (not the JSONL), a judged-bits change (the finding's `scoreEquivCp` example, or nodes/multiPv/hashMb/ttReset) trips CI EVEN IF every judgeParams stamp is migrated in place; only a deliberate edit to this literal — placed next to the "drift ⇒ regenerate" message — can re-green it. Tier-2 aggregation params (`lifetimeScheme`, the actual J7 drift) are deliberately EXCLUDED, so legitimate Tier-2 evolution never drifts this golden and never tempts a stamp migration. A cross-bind also pins every row's own `judgeNodes`/`judgeMultiPv` stamp to the FROZEN values (not merely to a matching digest string). Engine-free, deterministic; no PARAMS_A5 / corpus / anchors / golden change.

**Verified.** `CI=1 node scripts/test-judge-calibration.mjs` ALL GREEN — 73 assertions (+6 for A5-27: five judged-bits golden fields + the per-row stamp cross-bind), two runs byte-identical; `npm run typecheck` green (node/web/server). Non-vacuity by construction: the golden is a literal INDEPENDENT of PARAMS_A5, so any judged-bits param edit flips the corresponding `eq` red regardless of JSONL state — the exact failure scenario ("params change alters judged bits + a repeat of the migrate-the-stamps precedent ⇒ CI stays green") is now caught under CI. The pre-existing digest/stamp checks (`judgeParams uniform`, `== PARAMS_A5_DIGEST`, per-Tier1Record `params` pin) are retained.

**Residual.** Engine-free, this cannot PROVE the stored bits were produced under the current rules: the corpus carries only aggregated Tier1Records, no raw per-ply judge lines to re-aggregate at the live `scoreEquivCp`, so that bit-level proof remains the LOCAL engine receipt (§7, one honest row, skipped under CI per the test-judge-node convention). The strengthening moves the authoritative linkage OUT of the mutable JSONL into a reviewed in-test constant that is insensitive to Tier-2 drift, so a judged-bits regression can no longer stay green under a silent bulk stamp migration — but a developer who edits BOTH the corpus stamps AND this golden without regenerating still passes engine-free. A CI-runnable bit-level receipt needs raw judge lines stored in the corpus (engine regeneration) — deferred to the next J-pass. Separately, test-judge-calibration is in no CI workflow today (build.yml runs test-judge-fit/test-judge-node), an orthogonal wiring gap outside this file's lane.


### A5-28 — [FIXED] sidesOf() sort comparator is inconsistent on (idx, side) ties, and the multi-run thresh family has full ties — section-5 threshold-ε window selection and §7(a) fold order are implementation-defined, not spec-deterministic

- **File:** `scripts/test-judge-calibration.mjs`  ·  **Severity:** minor  ·  **Status:** FIXED 2026-07-22

**Fix.** `sidesOf()`'s tail comparator `a.side < b.side ? -1 : 1` returned +1 (never 0) on a full (idx,side) tie, so `cmp(a,b)===cmp(b,a)===1` — an inconsistent comparator ECMA-262 leaves implementation-defined; the thresh family's three append runs (mruav11j / j7w1 / j7w2, same i%2 subject-side rule) collide on many (idx,side) keys, so §5's `slice(0, 2K−1)` threshold-ε window and §7(a)'s sequential glicko fold depended on V8 TimSort internals. The comparator is now extracted as `sideCmp(a,b)` and made a strict TOTAL order: idx, then side (the `a.side<b.side` branch is UNCHANGED so honest/full/half ordering is byte-identical to before), then the globally-unique `gameKey` as a total tiebreak that resolves exactly the previously-undefined ties. A new "ordering determinism" section asserts `sideCmp` is a strict total order over each family (no adjacent pair 0-compares after sort) and that the thresh family genuinely carries (idx,side) collisions so the gameKey key is load-bearing, not vacuous. No PARAMS_A5 / anchors / corpus / golden change.

**Verified.** `CI=1 node scripts/test-judge-calibration.mjs` ALL GREEN — 73 assertions (+5: strict-total-order over honest/full/half/thresh + the load-bearing check reporting 134 genuine thresh (idx,side) collisions); two CI runs byte-identical. The thresh threshold-ε window z is now the DETERMINISTIC 3.495σ (was 3.359σ under V8's incidental order — inside the [3.359, 3.495]σ range the verifier enumerated, still < 5.0 and inside the escalate band [3.0, 5.0), so no assertion flips) and the §7(a) number is a stable 0.64 Elo/game. Cross-engine guarantee proven directly out-of-suite: replicating `sidesOf('thresh')` over the committed rows in FORWARD, REVERSED, and seeded-SHUFFLED input order yields the byte-identical 274-entry sequence (a strict total order sorts to a unique sequence regardless of input permutation or the host engine's sort algorithm). `npm run typecheck` green (node/web/server).

**Residual.** None in-lane. `sidesOf` is test-script-only (production Tier-2 windowing uses chain-ordinal order, unaffected), and the honest-family/anchors fits never hit a tie so their bytes are unchanged. The published §14-A5/§7(a) proof numbers are now reproducible on any conforming JS engine, closing the §0/§8 bit-determinism gap the finding named.


### A5-29 — [FIXED] The only cross-artifact judging receipt re-implements the transcript->positions mapping inline (bare fenBefore, one honest row), so no test can detect a judged-surface or encoding divergence between producers

- **File:** `scripts/test-judge-calibration.mjs`  ·  **Severity:** minor  ·  **Status:** FIXED 2026-07-22

**Fix.** The §7 engine receipt already routes its one local honest re-judge through the canonical `transcriptToJudgePositions` builder (A5-01, reachable via the judge barrel — `judge/index.ts` re-exports `./judge`), but §7 is skipped under CI, so on its own no CI test could detect a judged-surface/encoding divergence between PRODUCERS. A new engine-free "canonical judging surface" section now runs that SAME builder over EVERY corpus row of EVERY family and asserts it reproduces the exact surface the corpus was measured on: nPlies positions, ply i, bare FEN == `fens[i]`, NO `moves`/path field, contiguous from 0, no tail. A producer that judged a ply SUBSET, appended a TAIL (the in-repo gen-judge-corpus fenBefore+TAIL vs gen-cheater-corpus no-tail fork), or used the `position fen START moves …` encoding would diverge HERE — under CI, across the conviction-bearing families, not only the one local honest row. A non-vacuity assert shows the builder emits exactly moves.length positions, so a nPlies+1 tail surface cannot round-trip through it. No PARAMS_A5 / anchors / corpus / golden change; the routing needed NO non-owned-file edit (the barrel already exports the builder), so this is FIXED, not deferred.

**Verified.** `CI=1 node scripts/test-judge-calibration.mjs` ALL GREEN — 73 assertions (+4 for A5-29: builder-exported-from-barrel; canonical surface reproduced for all 510 rows (every ply, bare-FEN, no tail); all four families receipt-verified honest 176 / full 30 / half 30 / thresh 274; tail non-vacuity — a nPlies+1 producer surface (141) is not reproduced (140 == nPlies)); two CI runs byte-identical; `npm run typecheck` green (node/web/server). The receipt is now engine-free and CI-ACTIVE, and the §7 local engine receipt continues to route through the same canonical builder, so the byte-level re-judge and the structural surface check share one surface definition.

**Residual.** BIT-level engine reproduction still covers exactly one honest row (the local, CI-skipped §7 receipt); reproducing the cheater families' engine BITS needs the pinned WASM and is outside the engine-free scope of this pass. The new section proves cross-producer agreement on the judged SURFACE — which plies, what encoding, the finding's title — for all four families under CI, which is the layer a divergent producer would break. The gen-judge-corpus fenBefore+tail surface remains a documented NON-verdict residual (it feeds only the estElo fit; tier1Record already rejects its tail), unchanged here.


### A5-31 — [FIXED] Comment claims the J6 browser gate asserts the SAME frozen 9-position Tier-1 digest from the web adapter; the browser gate actually checks a 3-position subset against a live node oracle, never GOLDEN_T1_DIGEST

- **File:** `scripts/test-judge-node.mjs:131`  ·  **Severity:** minor  ·  **Status:** FIXED 2026-07-22

**Fix.** Comment-only defect — verdict behaviour was already correct; the two fixture comments that over-claimed J6's coverage are corrected and the actual split is now made executable. The JUDGE_POSITIONS header and the FROZEN GOLDEN header no longer say "cross-platform parity anchor … J6's browser gate asserts the SAME digest from the web adapter" / "the value the J6 browser parity gate must reproduce bit-for-bit"; they now state the truth the fixture's OWN docstring (accounts-fixture.mjs:395-400) already carried: GOLDEN_T1_DIGEST is the NODE-only cross-run/cross-machine anchor over all nine positions, while J6's browser ENGINE gate re-judges only the 3-position JUDGE_PARITY_POSITIONS subset (plies 4/20/60) and asserts a LIVE browser==node digest for it (local-only, skipped under CI) — so plies 6/16/40/50 and the two paths unique to the 9-set (Réti mate-in-2 ply 70, K<multiPv single-legal-reply ply 80) are pinned on NODE only. The stale "cross-platform parity anchor" label on the node-golden assertion message (the only other GOLDEN_T1_DIGEST mention) is corrected the same way. To stop the over-claim ever silently returning, a new pure/engine-free section 'A5-31 J6 browser-parity subset boundary' imports JUDGE_PARITY_POSITIONS from the fixture (kept the single source of the set) and pins the boundary as executable asserts: node freezes 9, browser re-judges 3, strict subset, each parity position byte-identical to its 9-set counterpart, node-only plies === {6,16,40,50,70,80}, and specifically that ply 70 + ply 80 are node-only. No code path, no PARAMS_A5, no wasm/golden touched — GOLDEN_T1_DIGEST is byte-identical (`F7XHAGNBkIR_2l6H7J8-Y2JTOOFj5SBtyYGvPAFIl3s`), no re-freeze; JUDGE_PARITY_POSITIONS export preserved.

**Verified.** New deterministic regression in scripts/test-judge-node.mjs (the pure 'A5-31 J6 browser-parity subset boundary' section, +8 asserts, no engine). Proven green out-of-engine by a standalone harness that imports the REAL JUDGE_PARITY_POSITIONS export and cross-checks its mirrored JUDGE_POSITIONS against the suite source (mirror == real plies 4,6,16,20,40,50,60,70,80): all 8 asserts GREEN; negative controls confirm the pre-fix scenario is now caught — the false "browser asserts the SAME 9-position digest" claim flips the `length === 3` assert RED, and pulling ply 70/80 into the browser subset flips the node-only assert RED. `node --check scripts/test-judge-node.mjs` PARSE OK; `npm run typecheck` GREEN on all three tsconfigs (node/web/server). Per the no-engine / don't-re-judge-games rule the full engine suite was NOT run here — the added section is pure and the lead's authoritative wall runs the engine gate.

**Residual.** The underlying coverage gap the finding names is unchanged by design (documented residual for a later J-pass): the web adapter is only ever exercised on the 3-position subset via a LIVE, CI-skipped browser==node comparison, so a correlated browser+node drift on those 3 positions still passes and plies 6/16/40/50/70/80 are never bit-pinned through the browser. This fix makes that boundary explicit and machine-enforced so it cannot be silently widened in prose again, but does not extend browser coverage to the full 9-set — that is browser wall-time work in test-web-accounts-browser.mjs (outside a comment-accuracy fix / this lane). The engine-parity section's CI skip (test-web-accounts-browser.mjs:282) is likewise unchanged.


### A5-32 — [FIXED] Operator peer's 'last-resort Tier-2' judge is the non-canonical A2 analyseFixedNodes surface, whose protocol and parse rules differ bit-wise from the canonical judgeGame path

- **File:** `server/operator/peer.ts:112`  ·  **Severity:** minor  ·  **Status:** FIXED 2026-07-22

**Fix.** startOperatorPeer now constructs its judge capability via the CANONICAL adapter `newNodeJudgeEngine` (server/judge/nodeAdapter.ts — existing module, imported, not edited), so `OperatorPeer.judge` is the shared `JudgeEngine` the canonical `judgeGame` drives, typed as such and doc-pinned "driven ONLY through judgeGame". The raw A2 `JudgeInstance` (analyseFixedNodes: per-POSITION ucinewgame+Clear Hash, bound-keeping depth+nodes-requiring parser, no fail-closed checks, no Threads pin) is no longer reachable from the peer handle, so A6 cannot wire Tier-2 through the wrong bit surface. The peer's redundant `assertWasmHash` pre-check was dropped: newNodeJudgeEngine's mandatory verifyWasmHash gate (typed JudgeWasmHashError, thrown before spawning) plus the wrapped newInstance's default re-verify are the two loud layers, and a failure is caught by the unchanged try/catch degrading to witness-only exactly as before (this also removes the old pre-check's quirk of hashing the DEFAULT shipped wasm when `enginePath` was supplied without `wasmPath`). `stop()` now calls `judge.close()` (adapter surface) instead of `quit()`. No cross-lane edit was needed; nodeAdapter/nodeEngine/params untouched; no signed/state payload, digest, or golden changed.

**Verified.** scripts/operator-smoke.mjs (the only suite bundling peer.ts) gained the A5-32 regression plus the surface cross-check: (a) SHAPE — the peer-held judge must expose the JudgeEngine send/onLine/onError/close surface and must NOT expose analyseFixedNodes/quit; counterfactually confirmed discriminating via an out-of-repo esbuild probe of a raw newInstance JudgeInstance, which fails both predicates (old wiring caught); (b) BITS — canonical judgeGame is driven THROUGH the peer-held handle over the existing 3-position JUDGE_PARITY_POSITIONS fixture (multi-position ⇒ the warm per-game-TT surface where the fork bit-diverged in the verifier's probe; moves-path opening + kiwipete FEN + mate-in-1) at a smoke-trimmed Tier-2 config (`{...judgeConfigForTier(2), nodes: 20000}` — the sanctioned `nodes`-ONLY test trim; multiPv/hashMb/ttReset stay pinned), and its judgeOutputDigest must equal a fresh canonical newNodeJudgeEngine re-run bit-for-bit — the finding's failure scenario ("the peer's verdicts mismatch every canonical re-run") is now the asserted invariant, and the smoke finally drives a real search through the peer's judge (closing this finding's slice of the line-343 coverage observation). Suites green: operator-smoke.mjs (17 assertions, ran twice — stable), npm run typecheck (all three tsconfigs).

**Residual.** The raw A2 surface (newInstance/analyseFixedNodes in server/judge/nodeEngine.ts) rightly remains for A2 loader-infra tests (test-judge-node golden vectors) and stays importable by anyone; its doc-comment's misleading "(spec §8 replay gate)" phrasing flagged in the verifier note lives outside this lane (judge lane) and is left for that owner. A6's actual Tier-2 mount — serving verdict requests through `op.judge` via judgeGame/tier2 records — remains A6 scope by design; the peer now hands it the correct surface.

### A5-33 — [FIXED] Orphaned doc block says the J3 estElo anchor set 'is what may feed T', contradicting the J6 rule that only TIER2_ANCHORS_JUDGE feeds T — and nothing at runtime blocks the biased set from being injected

- **File:** `src/shared/accounts/judge/anchors.ts`  ·  **Severity:** minor  ·  **Status:** FIXED 2026-07-22

**Fix.** The dangling J3 doc block (ex-lines 128-136, orphaned above the J6/TIER2_ANCHORS_JUDGE insertion) was removed and re-attached — corrected — directly to `TIER1_ANCHORS_JUDGE`: the provenance sentences are kept verbatim, and the stale 'THIS set (not TIER1_ANCHORS_PROVISIONAL) is what may feed T' instruction is replaced by an explicit ROLE paragraph: estElo anchor set ONLY (spec §8 Tier-1's judge-config refit); it must NEVER be injected as a Tier-2 ACPL anchor (`Tier2Anchors.acpl`) because J6 measured it at +15.9cp mean bias on the Tier1Record statistic — as a Tier-2 anchor it deflates honest devAcpl below zero, blunting detection and desyncing evaluators from the canonical escalation/T trigger (the verifier's sign-corrected impact, not the finding's false-escalation narrative); Tier-2 windows and T feed ONLY from TIER2_ANCHORS_JUDGE (tier2.ts header contract). Because `checkTier2Anchors` is shape-only BY DESIGN (receipts must re-verify historic verdicts under the digest-pinned bundle they carry — a hard runtime pin would break that, per both reviewers), the prohibition is additionally carried IN the value: `TIER1_ANCHORS_JUDGE.fit` now ends '; estElo-only — must not feed Tier-2/T (J6: +15.9cp bias on the Tier1Record statistic; the only Tier-2/T bundle is TIER2_ANCHORS_JUDGE)' — the exact convention TIER1_ANCHORS_PROVISIONAL already uses ('must not feed T'), so the guard travels with the value at runtime instead of living only in source prose. No knot/σ/config integer changed; the +15.9cp figure is recomputed by the calibration suite's J3 cross-check (prints 15.88cp), so no new unpinned numeric claim is introduced (A5-34 class avoided).

**Verified.** Deterministic probe (esbuild-bundles the real anchors.ts + tier2.ts, no engine; 12/12 PASS): the stale 'is what may feed T' text is gone from the file; a doc block is directly attached to `TIER1_ANCHORS_JUDGE` stating the NEVER rule, naming TIER2_ANCHORS_JUDGE, and citing the measured bias; the fit tag carries 'must not feed Tier-2/T' while still naming the params digest and not matching 'J3-REFIT-PENDING' (the two existing test-judge-fit pins); the J6 block's own contract line ('THIS bundle feeds Tier-2 windows and T') is intact so the two docs now agree; `acplByElo` knots, sigmaAcplMicro (21_902_355), nodes and multiPv are byte-identical; and `tier2AnchorsDigest(TIER2_ANCHORS_JUDGE)` is unchanged at `lesqBEHtNzakeh94uBiOsQzPA-KS_tn_nXwTZ7ClhA8` (captured pre-edit) — no verdict-receipt or golden drift is possible from this change. Suites green: test-judge-fit.mjs (67), test-accounts-tier2.mjs (263, frozen goldens intact), CI=1 test-judge-calibration.mjs (55 — its J3 cross-check prints bias 15.88cp, the doc's cited figure recomputing from the corpus), npm run typecheck (node+web+server, exit 0).

**Residual.** (1) Runtime injection-openness stands BY DESIGN (verifier + relevance judge concurrence): anchors stay injectable so historic verdict receipts re-verify under the exact digest-pinned bundle they were computed with; the doc + fit-tag marker are the binding guard, and A6's ban-trigger wiring must bind TIER2_ANCHORS_JUDGE. (2) A permanent suite pin of the marker (one line in scripts/test-judge-fit.mjs, e.g. `ok(T.fit.includes('must not feed Tier-2/T'))`) is outside this lane (anchors.ts only) — a cheap lead follow-up if wanted; the marker survives both existing fit-tag assertions today. (3) The J6 provenance block's stale honest-z numbers (1.884σ/1.932σ) were deliberately NOT touched — that is finding A5-34, same file, separate fix.

Z7ClhA8` (captured pre-edit) — no verdict-receipt or golden drift is possible from this change. Suites green: test-judge-fit.mjs (67), test-accounts-tier2.mjs (263, frozen goldens intact), CI=1 test-judge-calibration.mjs (55 — its J3 cross-check prints bias 15.88cp, the doc's cited figure recomputing from the corpus), npm run typecheck (node+web+server, exit 0).

**Residual.** (1) Runtime injection-openness stands BY DESIGN (verifier + relevance judge concurrence): anchors stay injectable so historic verdict receipts re-verify under the exact digest-pinned bundle they were computed with; the doc + fit-tag marker are the binding guard, and A6's ban-trigger wiring must bind TIER2_ANCHORS_JUDGE. (2) A permanent suite pin of the marker (one line in scripts/test-judge-fit.mjs, e.g. `ok(T.fit.includes('must not feed Tier-2/T'))`) is outside this lane (anchors.ts only) — a cheap lead follow-up if wanted; the marker survives both existing fit-tag assertions today. (3) The J6 provenance block's stale honest-z numbers (1.884σ/1.932σ) were deliberately NOT touched — that is finding A5-34, same file, separate fix.


### A5-34 — [FIXED] TIER2_ANCHORS_JUDGE provenance claims honest max z 1.884σ / held-out 1.932σ / margin ≥1.07σ — the committed suite over the committed corpus deterministically measures 1.482σ / 2.123σ (held-out margin 0.877σ)

- **File:** `src/shared/accounts/judge/anchors.ts:151`  ·  **Severity:** minor  ·  **Status:** FIXED 2026-07-22

**Fix.** The J6 provenance comment now states the measured reality, not the stale draft numbers. Ground truth was re-derived deterministically from the COMMITTED corpus with no engine work: `CI=1 node scripts/test-judge-calibration.mjs` (three runs, byte-identical stats) plus an exact-precision replication of the suite's section-3 code paths (same esbuild bundling, same sidesOf/pav/fitAnchors/trailing-K scan; /tmp/a5-34-measure.mjs) to capture the full integer micro-σ values: in-sample max trailing-30 z = 1_482_160 (1.482σ, chunk max 1_184_333), split-half held-out max = 2_123_366 (2.123σ over 176 odd-game sides), margins to the 3.0σ escalation trigger 1_517_840 in-sample and 876_634 held-out. anchors.ts:151-160 now carries: the corrected σ figures with their exact micro integers, conservative floor-rounded margin claims (≥ 1.51σ in-sample, ≥ 0.87σ held-out — the prior note's "≥ 1.07σ" was itself a round-UP of 1.068), the re-measure date + a "prior 1.884σ/1.932σ/≥1.07σ figures did not reproduce" note, and an explicit CAUTION that the suite PRINTS these z stats but asserts only z < 3.0σ (does not pin them) — re-run the suite, do not cite the comment, when tightening zEscalateMicro or signing off the honest-FPR proof. The header's "recomputes every integer below" was tightened to "every integer in the bundle below" (anchors.ts:139) so the suite's enforcement scope (integer-exact knots/sigmas round-trip, §2) can no longer be over-read as covering the comment's z stats. Comment-only change: zero behavior, zero digest/golden impact, TIER2_ANCHORS_JUDGE integers untouched.

**Verified.** Determinism of the measurement: three suite runs print identical stats (max honest trailing-30 z = 1.482σ, split-half held-out 2.123σ, margins 1.518σ/3.518σ), and the /tmp replication reproduces them at micro precision. A scripted cross-check parses the updated anchors.ts comment and asserts every figure (both σ values, both exact micro integers, the 876_634 margin) equals the measured values and that the stale 1.884/1.932 figures survive only inside the did-not-reproduce note — GREEN. Repo-wide grep: the only other occurrence of the stale figures is STATUS.md:544, which is the A5-34 tracking entry itself (already stating 1.884→1.482), not a stale claim. Suites green: test-judge-calibration.mjs CI sections (55, engine receipt CI-skipped by design), test-judge-fit.mjs (67), test-accounts-tier2.mjs (263), npm run typecheck (all three tsconfigs, exit 0).

**Residual.** The suite still only BOUNDS the honest z stats (< 3.0σ at test-judge-calibration.mjs:332,346) — pinning them (eq of §3 block-1 maxZ to 1_482_160 and §3 block-2 held-out maxZ to 2_123_366) is a 2-line change in scripts/test-judge-calibration.mjs, which is outside the anchors lane; the exact micro integers now live in the anchors.ts comment so the suite lane/lead can add the pin without re-measuring. Until then the comment's CAUTION line is the guard against re-staling.

Z to 1_482_160 and §3 block-2 held-out maxZ to 2_123_366) is a 2-line change in scripts/test-judge-calibration.mjs, which is outside the anchors lane; the exact micro integers now live in the anchors.ts comment so the suite lane/lead can add the pin without re-measuring. Until then the comment's CAUTION line is the guard against re-staling.


### A5-36 — [DEFERRED] The §8 strength-trajectory Tier-1 signal is computed and unit-tested but never persisted in Tier1Record nor consumed by Tier-2/T — the smurf / rapid-improvement channel the spec mandates is absent from every verdict

- **File:** `src/shared/accounts/judge/tier1.ts`  ·  **Severity:** minor  ·  **Status:** DEFERRED — **RATIFIED 2026-07-22** (owner-delegated): persistence landed; the verdict weight stays deferred because wiring an uncalibrated rapid-improvement signal into z would flag exactly the honest improving players the prime directive protects. Awaits the J4/J6 calibration that measures the honest slope distribution.

**Fix (partial — persistence landed; verdict weight DEFERRED).** `tier1Record` now OPTIONALLY persists the §8 slope per side as `Tier1Side.trajectoryMicro`. A new trailing arg `priorAcplMicros?: { w?: readonly number[]; b?: readonly number[] }` carries each account's PRIOR-game acpl window (oldest→newest, excluding this game); when a side's window is supplied, `tier1Side` appends this game's freshly-derived `acplMicro` as the newest point and stores `trajectoryMicro([...window, thisAcpl])` — the slope of the window ENDING at this game (negative = strengthening). This makes `trajectoryMicro` a live call site (it was dead code) and co-locates the signal with the game. It is the record's lone optional field: a side with no window — every current caller — carries NO field, and because cjson skips `undefined` members (codec.ts:26-28,118-120) the record is byte-identical to the pre-A5-36 one, so NO frozen `tier1Digest` re-froze anywhere. This is the exact rdMicro (A5-02) / incMs (A5-15) "optional derivation input, absent ⇒ byte-identical, awaiting calibration" discipline. Shape validated fail-closed (`priorAcplMicros` must be a plain `{w?,b?}` object — non-object/null/array rejected; each side a number array; contents guarded by `trajectoryMicro`'s existing range/window checks). No PARAMS_A5 change (no digest drift); the `Tier1Side`/`Tier1Record` change is additive-optional, so tier2.ts's `checkTier1Side` (reads only `scored`/`acplMicro`/`matchMicro`, ignores extra fields) and every synthetic golden are untouched.

**Verified (deterministic, no engine).** New regression scripts/test-accounts-tier1.mjs §(f2) (20 asserts): the finding's exact scenario — an account whose per-game ACPL trends sharply downward (prior `[200,180,160]e6`, this game `150e6`) — now persists `w.trajectoryMicro = −17_000_000` (strengthening, the smurf direction), while a rising window persists `+17_000_000` (sign live both ways); per-side opt-in is independent (w supplied ⇒ field, b omitted ⇒ no field); byte-safety pinned three ways (no-window record still the frozen `chzY1umBfAfE9M6Ce6u_73xtvCaMEIwOK7dw_mKVm-8`; `priorAcplMicros` absent ≡ explicit `undefined` ≡ empty `{}` — bytes unchanged); the persisted slope EQUALS `trajectoryMicro(window ++ this-game acpl)`, i.e. the deterministic window→slope map a J4 consumer would compute from the per-game `acplMicro` every record already carries; the with-window record freezes a NEW golden digest `GQCr2JGq_ScZAHyBcRLw17zd0kiQYLQMKeLu6ka7h30` (this file only); and a fail-closed matrix rejects a non-object / array / non-array-side / negative / over-`MAX_CPL_MICRO` / over-`TRAJ_MAX_WINDOW` input. Suites green: test-accounts-tier1.mjs (264 asserts — all pre-existing goldens incl. the A5-01/A5-37 frozen digests intact), test-accounts-tier2.mjs (263 asserts — all tier2 goldens intact, the optional field perturbs no consumed Tier1Record byte), `npm run typecheck` (node+web+server). Engine corpora (test-judge-node `GOLDEN_T1_DIGEST`, test-judge-calibration receipts, accounts-fixture `tier1RecordDigest`) NOT re-run per the no-re-judge rule — each is minted via `tier1Record` with no window arg, so byte-invariance guarantees the frozen digests hold un-refrozen.

**Residual (the deferred half — the verdict weight).** The signal is now stored and the consumption MAP is deterministic + tested, but no verdict READS it: folding a σ-per-slope term into the Tier-2 z (`gameDevMicro`/`aggregateZMicro`) or the trust T (`mm/trust.ts`) is a calibration dial with a false-positive tradeoff (§0 no-false-fraud), and both call sites are OUTSIDE the tier1 lane. Wiring it uncalibrated is exactly the hazard this brick must not ship, so it awaits the J4/J6 refit that measures the honest-corpus slope distribution and sets the weight/σ. Populating the field in production also needs the minting caller (gen-cheater-corpus / the A6 record producer — cross-lane) to thread each account's chronological prior-acpl window, and the precise windowing (per-account color handling, length ≤ `TRAJ_MAX_WINDOW−1`) is itself a J4 design choice. Until both land the field is present only in the §(f2) fixtures — inert but available, and byte-free for every legacy record.


### A5-37 — [FIXED] tier1Record accepts a JudgeOutput judged at ANY (nodes, multiPv, hashMb) — only the params digest is checked, and the record drops the config, so wrong-config signals are indistinguishable without a full re-judge

- **File:** `src/shared/accounts/judge/tier1.ts`  ·  **Severity:** minor  ·  **Status:** FIXED 2026-07-22

**Fix.** tier1Record now carries a canonical-config gate immediately after the params-digest gate: it rejects fail-closed (Tier1InputError) any JudgeOutput whose `(nodes, multiPv, hashMb)` is not the Tier-1 fixed-node config `judgeConfigForTier(1)` = `(PARAMS_A5.t1Nodes 200000, PARAMS_A5.t1MultiPv 4, PARAMS_A5.hashMb 16)`. Because PARAMS_A5_DIGEST folds `t1*` and `t2*` into ONE digest, the pre-existing `out.config.params === PARAMS_A5_DIGEST` check could not separate a Tier-1 output from a Tier-2 (`t2Nodes` 2M / `t2MultiPv` 6) or a degenerate (`nodes=1`) one — all carry the identical params echo. With `params` already gated, the config echo (`JudgeConfigEcho` = `{nodes, multiPv, hashMb, params}`) is now pinned in EVERY field. Deliberate scope: the gate is NOT a new record field. The record already commits to the WHOLE JudgeOutput — config included — through its `judge` field (`canonicalHash` covers `config`), so pinning the config at the production boundary is exactly what makes a wrong-config record both unmintable (production throws) and unverifiable (a verifier's own `tier1Record` recompute re-runs the gate and throws instead of reproducing junk z-scored against the 200k/MPV4-fit anchors). Because no field was added, no accepted-path bit changes and no `tier1Digest` moves. The gate sits at the `tier1Record` trust boundary only (parallel to the existing params gate and the A5-01 full-coverage gate); `sideMoveScores`/`clockSamplesForSide` stay config-agnostic diagnostics primitives per the module's documented contract. The module header and the `tier1Record` doc comment now state the config refusal; no PARAMS_A5 edit, no digest drift.

**Verified.** New deterministic regressions in scripts/test-accounts-tier1.mjs §(j) (synthetic JudgeOutputs, no engine): the finding's exact scenario — a Tier-2-config output (`nodes=2000000, multiPv=6, hashMb=16`) with the identical PARAMS_A5_DIGEST echo — is REJECTED with Tier1InputError, as is the named degenerate output (`nodes=1 / MPV6 / Hash1`); each config field is isolated (`nodes` off-by-one 199999, `nodes` 100000, `multiPv` 6, `hashMb` 8, `hashMb` 32) and each REJECTED, every assertion additionally checking the error message contains "not the Tier-1 config" so the CONFIG gate — not the line-count or params check — is proven the rejecter (MPV/Hash overrides kept ≥ the fixture's 4 lines so `checkJudgeOutput` passes first). Positive controls: the fixture CFG is asserted to be the genuine Tier-1 config, the canonical-config record is still ACCEPTED, and the frozen `tier1Digest` `chzY1umBfAfE9M6Ce6u_73xtvCaMEIwOK7dw_mKVm-8` reproduces byte-for-byte (gate added no field, changed no accepted-path bit). Suites green: test-accounts-tier1.mjs (244 asserts, was 232), test-accounts-tier2.mjs (263, frozen goldens intact — it builds Tier1Record literals and never calls tier1Record, so it is untouched), operator-smoke.mjs (20 — the accounts-fixture parity path that calls tier1Record at the canonical config still passes; JUDGE_PARITY_POSITIONS preserved), npm run typecheck (all three tsconfigs). Grep confirms tier1Record is the sole Tier1Record constructor, so the gate closes the class at its only production site.

**Residual.** The finding's optional second half — echoing `(nodes, multiPv)` INTO the Tier1Record — is deliberately NOT done, and is unnecessary. Given the fail-closed gate every honest-path record is guaranteed canonical-config, so an echoed pair would be a constant, and the record already binds the exact config through its `judge` digest; adding a field would drift every frozen `tier1Digest` and force the coordinated cross-lane re-freeze of the J6 corpus receipt (test-judge-calibration.mjs pins tier1Record digests bit-for-bit) plus the accounts-fixture and browser-parity goldens — all outside this lane, the same coordinated event the header reserves for the A5-14 `matchedBest` promotion. Adversarially the echo is not load-bearing (a forger forges any field; re-judge reproducibility via the stored `judge` digest is the real authority), so nothing is lost by omitting it. A caller still wanting a non-canonical diagnostic can use `sideMoveScores`/`clockSamplesForSide` (config-agnostic by contract), but such output can never form a record. The estElo-fit corpus surface (gen-judge-corpus) is unrelated to this gate and already covered by the A5-01 tail rejection.


### A5-38 — [FIXED] a4-v1 `bans` checkpoint state is unbounded in attacker-controlled ladder strings

- **File:** `src/shared/accounts/ratings/fold.ts`  ·  **Severity:** minor  ·  **Status:** FIXED 2026-07-22

**Fix.** `banStep` now installs/extends a `bans` entry ONLY for a ladder present in the account's `state.ladders` — a ladder it has actually folded a rated game in. A third `ladders` param was threaded from the step's pre-step `state.ladders` (a `selfban` event never updates `ladders`, so pre == post; for a `segment` banStep short-circuits on `type` before touching the arg, so the pre-step map is always the correct membership set), and the guard `if (!Object.prototype.hasOwnProperty.call(ladders, p.ladder)) return bans` sits right after the payload parse, before the A5-22 expiry derivation. This ties **|bans| ≤ |ladders|**: a ban entry now costs one WITNESSED rated game (verifySegmentEvent + an M-of-N-cosigned oppCkpt) — the exact bound `ladders` already carries — instead of a free selfban (verifyChain has no per-`selfban` rule and verifies no witness countersignature, so the 1..64-char `zSelfBanPayload.ladder` namespace was adversary-minted at zero third-party cost). It drops NO legitimate ban: a Tier-2 conviction needs a full `reganK`-game window on that ladder and §8 orders the selfban after the window-completing rated game, so a compliant self-ban's convicting ladder is always in `ladders` when the selfban folds; a selfban naming an unplayed ladder is meaningless (no rated state there to gate) and is ignored like the existing malformed/wrong-lane cases. Deliberately **membership**, not ladderId-shape validation — shape alone (`${kind}:${Category}`) does not bound cardinality because `kind` is a free 1..32-char registry string (`k1:Blitz`, `k2:Blitz`, … stays O(N)); only the witness-bound `ladders` domain bounds it. No PARAMS_A5/PARAMS_A4 change (no digest drift), no new tunable/constant, no goldens re-frozen (ban VALUES are unchanged — only WHETHER an unplayed-ladder selfban folds). Determinism / integers-and-strings-only / no-clock / fail-closed preserved; the A5-22 derived-and-monotonic expiry is untouched and composes (the membership gate precedes it). Only 2 of the 3 in-lane files were touched (`judge/tier2.ts` needed no change).

**Verified.** New deterministic regression in `scripts/test-accounts-tier2.mjs` §7 (synthetic events, no engine): seed exactly ONE played ladder, fold a selfban on it (folds → 1 entry), then fold 64 distinct schema-valid WITNESSED selfbans each naming a fabricated UNPLAYED ladder → **ZERO** new entries (pre-fix: +64), the fabricated ladders never appear in `bans`, `|bans| ≤ |ladders|` holds, `byType.selfban` still ticks all 65 (counters are ungated — only `bans` folding is gated), and a genuine later selfban on the played ladder still folds through the gate and monotonically extends it (window → 4). The pre-existing §7 fold-bans and A5-22 fixtures were reseeded so their selfban ladders fold onto a state that carries those ladders via `ladderInit()` (the faithful "prior rated games folded" precondition; a real rated segment needs a witness sig + M-of-N oppCkpt, heavy to synthesize) — no assertion added or removed there, and every ban VALUE is byte-unchanged, so the `hashBefore`/independent-refold determinism checks and the A5-22 derived+monotonic (no self-un-ban) assertions all hold verbatim. Suites green: test-accounts-tier2.mjs (300 asserts, +7 A5-38, all pre-existing goldens intact), test-accounts-ratings.mjs (300 — the fold.ts change is inert: no suite outside tier2 folds selfban events), test-accounts-chain.mjs (164), `npm run typecheck` (all three tsconfigs).

**Residual.** The `ladders` sub-map's own growth (one witness-bound rated game per distinct `${kind}:${category}`, `kind` a free registry string) is out of scope for A5-38 — which targets the strictly CHEAPER `bans` vector — and is the finding's own accepted caveat; `bans` now inherits exactly that (already-accepted) bound, never a worse one. The fold assumes a compliant client sets `selfban.ladder` to the canonical `ladderId(kind, tc)` (the same string the fold keys `state.ladders` by) — the A6 client-wiring contract; `makeSelfBanPayload` has no production caller yet, and worst-case nonconformance (a self-ban on a never-played ladder) yields only an inert, ignored ban, never a false conviction — the same "compliant-client" assumption class the A5-22 ts-derivation already documents. No hard numeric cap was added: it would need an arbitrary `[A5-CALIBRATED]` constant with awkward eviction semantics, whereas tying to the witness-bound `ladders` domain is the stronger, param-free bound and matches the module's discipline (bounds tied to real event costs, like `pruneSeen`/`prunePair`).


## Appendix — considered & dropped by the unbiased round (5)

Surfaced by search/implication but dropped by the independent verifiers — refuted as not-a-defect,
judged not-relevant/incongruent to the A5 goal, or a documented-accepted residual.

- **A5-05** (`scripts/test-accounts-tier1.mjs:379`) — Match-window tests pin the F6 degeneracy as goldens: a side playing 300cp-inferior rank-2 moves half the time is frozen at matched 10/10, matchMicro 1e6 — no test asserts any in-list move can be unmatched
  - **Dropped:** REFUTED — Every cited fact reproduces exactly: scripts/test-accounts-tier1.mjs:378-380 pins matched=10/matchMicro=1e6 ('in-list at any rank') for a side averaging 150cp ACPL via 5/10 rank-2 300cp-loss moves (fixture lines 348-368); lines 183/189/196 pin FIX_A's rank-4 loss-30 move as matched; suite is ALL GREEN (165 asserts). Runtime probe (/tmp/probe-a505.mjs over the bundled judge barrel) confirms the structural invariant: tier1.ts:451-452 (in-list self-scores own line) + tier1.ts:488/257-262 (window vs ANY line) make an in-list 950cp blunder matched, the worst move in a fully-listed 3-legal-move position matched (judge.ts:31-32 full listing), and even a not-in-list move 955cp below best matched via the rank-4 line's window — so no in-list-unmatched test could exist. HOWEVER, the defect adjudication fails: (1) the any-line semantic is the explicitly documented frozen parameter, docs/building/ACCOUNTS-PARAMS.md:92 'engine-match counts any move within ±15cp of a MultiPV line' [A5-CALIBRATED], and tier1.ts:58-64 documents in-list distance-0 matching as normative — spec §8 (ACCOUNTS-SPEC.md:296) is ambiguous and never mandates best-relative matching; the finding's 'direction that matters' presupposes an interpretation the spec does not state. (2) The window is live and tested both directions where it is decisional (not-in-list: lines 190, 205-216, 217-237). (3) No §8 property breaks: J6 calibration measured the honest baseline under this exact statistic (anchors.ts:190-205, match 0.599@400→0.939@2700, σ=62,335 — not saturated) with honest max trailing-30 z=1.884σ vs 3.0 escalation (anchors.ts:161); an engine-best cheater still convicts even at the 2700 ceiling (≈0.98σ match + 0.88σ acpl → z(30)≈5.1≥5.0). The residual 'signal wasted on inferior in-list moves' critique is companion finding A5-14 (semantics), not a test defect; golden re-freezing after any semantic change is ordinary maintenance and would necessarily accompany a J6 anchor refit. As filed against the test suite, this is not a defect — the suite correctly and completely pins the documented, calibrated semantic. If A5-14 is independently CONFIRMED, A5-05 reduces to a minor derivative maintenance note, not a standalone major.  ·  Facts verified: test-accounts-tier1.mjs:378-380 does freeze matched=10/matchMicro=1e6 over 5×300cp rank-2 moves ("in-list at any rank"), tier1.ts:451-452 + isEngineMatched (tier1.ts:254-260, any-line window) do make every in-list move self-match at distance 0, and matchMicro is a Tier-2 z input (tier2.ts:562). But the finding misreads the design: the self-match rule is the explicitly documented contract, stated twice in the tier1.ts normative header (tier1.ts:26-28 "String equality here only SELECTS the score source; it is never itself the match criterion"; tier1.ts:62-63 "An in-list move matches its own line at distance 0"), and the golden's own label "in-list at any rank" shows the test encodes intent, not an overlooked degeneracy. The spec one-liner (ACCOUNTS-SPEC.md:296 "score-equivalence window ... never exact-move matching") pushes in the permissive direction (don't require the exact move) and nowhere mandates a best-line-only window that could unmatch listed moves; demanding a test that "an in-list move far below best is NOT matched" asks for behavior the implemented contract deliberately lacks — such a test would simply fail against correct code. No A5 pillar is threatened: verdict bits are bit-identical either way (determinism intact), the fold stays pure, and the Tier-2 astronomically-low-FPR bound is unaffected because matchByElo/sigmaMatchMicro anchors (tier2.ts:468,562) are fit from corpora generated under the identical semantics, so uniform baseline inflation is absorbed by calibration — the residue is statistical power in low-branching positions, which is the substance of the separate semantic finding (A5-14), not of this test-side rider. "Goldens must be re-frozen if A5-14 is fixed" is routine golden-test maintenance (loud failure forcing conscious re-freeze is change-detection working as intended), not an independent defect. Not on the documented-accepted list, so knownAccepted=false; judged not relevant and not congruent as a standalone finding.
- **A5-19** (`src/shared/accounts/judge/tier2.ts:544`) — Any PARAMS_A5 change (even non-judged-bits fields) makes escalationDue THROW and aggregateZMicro/verifyTier2Verdict REJECT every validly-judged pre-migration Tier1Record — the deterministic trigger becomes un-evaluatable across the scheduled calibration re-pin
  - **Dropped:** judged not-relevant/incongruent  ·  Mechanics verified accurate (tier2.ts:544-545 live-digest throw inside aggregateZMicro/escalationDue per-entry checks at tier2.ts:741-743; verifyTier2Verdict rejection at tier2.ts:971; same gate at tier1.ts:590-593; whole-object digest at params.ts:47; real kkHGAC->a4eNbk drift per anchors.ts:33 vs scripts/lib/accounts-fixture.mjs:383-386). But the finding misreads the design on three counts. (1) Its core claim — that surviving a re-pin requires re-judging historical transcripts, "which is NOT a pure recomputable fold" — inverts the architecture: transcripts are the owner-signed primary data and Tier1Records are derived caches; re-judging from transcripts is exactly the pure recomputable fold §0/§8 guarantee, and it is the SANCTIONED, already-exercised migration rule recorded after the very drift the finding cites (STATUS.md:511-514: "Tier1Record-bearing corpora are RE-JUDGED from stored transcripts — J7 re-judged all 280 pre-drift games, 0 signal mismatches"). (2) The "fields that affect no judged bit" premise is wrong: zEscalateMicro/zThresholdMicro/reganK/selfBanDays define the Tier-2 trigger, conviction, window size, and ban term, and the full-set digest is explicitly deliberate (types.ts:74 "names the full parameter set, not just the knobs above"; params.ts:5-6). Admitting old-digest records into a current-rule-set window would reintroduce the A4-04 rule-set-ambiguity/honest-verifiers-disagree class; the fail-closed throw parallels the adjacent "unjudged rated games are non-compliant" gate (tier2.ts:738) and protects the single-canonical-rule-set invariant. (3) The failure scenario's stranded live state cannot arise on the scheduled path: the calibration re-pin lands BEFORE A-final (params.ts:3-4) and interim server accounts are only superseded at A-final (ACCOUNTS-SPEC §14), so pre-migration Tier1Records/self-bans are test artifacts covered by the recorded re-judge rule, not live accounts. A hypothetical post-launch re-pin would need a params-epoch mechanism, but none is scheduled, that is beyond A5 scope, and the digest-named-rule-set design keeps old receipts tamper-evident and reconstructable regardless. Not among documented-accepted residuals (a)-(g) (knownAccepted=false), but it collides with a documented in-force resolution rule, so it fails congruence; the implied fix (tolerating superseded digests or splitting the digest) would weaken, not advance, the A5 goal.
- **A5-25** (`scripts/test-accounts-tier2.mjs:251`) — The §7b salt suite asserts 'different grant subsets => different salts' — pinning the boundary-grinding degree of freedom instead of testing salt uniqueness per (root, ladder, window)
  - **Dropped:** REFUTED — Reproduced: scripts/test-accounts-tier2.mjs:248-252 does assert mkReveal(1,[0,1]) vs mkReveal(1,[1,2]) yield different salts (suite green). But this pins the explicitly documented derivation contract, not an accidental behavior: tier2.ts:51-57 states "The REVEAL RECORD is the canonical fact fixing WHICH grant subset defines the salt (individual ed25519 grants have no unique threshold aggregate); verdict-side determinism is 'given the salt reveals'", matching the module contract at tier2.ts:24-25. The invariant the finding demands (salt = unique function of root/ladder/window/witness-set) is not in ACCOUNTS-SPEC §7b (docs/building/ACCOUNTS-SPEC.md:264-267 requires only unpredictable-before + recomputable-from-reveal-after) and is cryptographically unattainable with individual ed25519 grants absent the full grant directory — the directory-snapshot dependency explicitly deferred as documented-accepted (e). The finding's mechanical claim is wrong: no within-reveal canonical-subset rule in countedGrants (tier2.ts:344-367) could make the two reveals converge — they carry disjoint signature material — so the assertion would still pass; only a strict full-population rule (needs the deferred snapshot, and would break every golden salt too) could break it. The failure scenario (two honest verifiers, different reveals, diverging J7 verdicts) is not A4-04: the salt depends only on witness-signed bytes inside the published reveal record, verifiers holding the same record agree bit-for-bit (test line 244), and which single record is canonical for a window is the reveal-publication layer assigned to A6/embedder (tier2.ts:54-57; judge.ts consumes no reveals — tier2.ts:413-414 caller-closed offsetOf). The residual subset-multiplicity/grinding concern is the cited underlying design finding (A5-18), double-counted here as a test defect.  ·  Verified concretely: scripts/test-accounts-tier2.mjs:248-252 asserts windowSalt(mkReveal(1,[0,1])) !== windowSalt(mkReveal(1,[1,2])) — two threshold-valid reveals for the same (root,ladder,window) — with the label 'the REVEAL is the canonical fact'; both windowSalt calls succeeding proves both reveals are valid (windowSalt throws on unproven reveals, tier2.ts:371-384). The divergence is load-bearing, not cosmetic: countedGrants (tier2.ts:344-367) counts only the grants the reveal carries, SaltReveal (tier2.ts:258-269) is an unsigned bag of grants anchored nowhere (no schema in events.ts/types.ts), and salt -> saltOffset (407-411) -> windowBounds (425-435) -> closed-window z list -> J7 lifetime verdict. So two honest auditors holding different valid reveals derive different verdict bits with no attributable tamper evidence — exactly the A4-04 consensus-split class §0 forbids — and the free choice among >=threshold subsets (plus wts re-signing freedom, tier2.ts:315-320) is a real boundary-grinding degree of freedom against §7b at the verdict-partition level. The design header (tier2.ts:52-55) admits ed25519 has no unique threshold aggregate and makes determinism conditional 'given the salt reveals', but nothing in A5 pins which reveal wins when several valid ones exist, and no DOCUMENTED-ACCEPTED item covers this: (e) concerns witness-set MEMBERSHIP validation via directory snapshot, not canonical-subset/canonical-reveal selection; (d) concerns corpus re-exercise. The finding does not misunderstand the design — it correctly cites the module's own framing and shows the suite pins the divergence-enabling behavior (any future canonical-subset rule would fail this assertion). Fixing it (testing salt uniqueness per (root,ladder,window,witness set), or at minimum pinning/documenting a deterministic canonical-reveal rule) directly protects bit-deterministic reproducible verdicts and §7b recomputability-after. Relevant, congruent, not a known-accepted residual; minor severity is appropriate since this is the test-surface symptom of the underlying derivation issue.
- **A5-30** (`scripts/test-judge-calibration.mjs:532`) — Clock forensics has zero non-synthetic coverage: all 510 corpus records carry clockFitMicro = 0 (flat-0ms clocks by construction), the receipt reproduces only because it re-zeroes clocks, and no fixture exercises increment-credited snapshots
  - **Dropped:** known-accepted residual  ·  Every factual claim verified: test-judge-calibration.mjs:532 rebuilds receipt moves with clockMs {w:0,b:0} matching gen-cheater-corpus.mjs:331; a scan of all 510 judge-calib-corpus.jsonl rows finds clockFitMicro=0 as the sole value on both sides (tier1.ts:330 returns 0 when total think T===0, the 'machine pacing — maximal misfit' branch per tier1.ts:102-103); test-accounts-tier1.mjs:152-158 and 356-368 fixtures decrement snapshots with no increment credit, while production stores post-afterMoveCredit clocks (mpSession.ts:1349) whose negative raw deltas tier1.ts:92-93 clamps at 0. However, this is the test-coverage face of documented-accepted residual (a): clockFit is emitted but uncalibrated and is NOT a Tier-2 z input, so nothing in the A5 verdict path consumes it. No A5 invariant is threatened — clockFitMicro=0 is bit-deterministic, receipt digests reproduce exactly, the fold stays pure over signed bytes, and only Tier-2 convicts. The failure scenario is wholly contingent on a future promotion that (a) explicitly defers, and the required calibration pass at that point would immediately expose an honest baseline degenerate at 0 (honest=bot=0, no separation). The finding is congruent (it correctly models the design and self-cites the (a) boundary) but fixing it now does not advance the A5 end goal; the sharper increment-distortion risk it invokes is sibling finding A5-15, judged separately.
- **A5-35** (`src/shared/accounts/judge/anchors.ts:188`) — TIER2_ANCHORS_JUDGE records fit-time params digest kkHGAC…, but the committed calib corpus it is measured on is 100% stamped a4eNbk… — and the suite enforces the wrong label, freezing it into the anchors digest embedded in every verdict
  - **Dropped:** judged not-relevant/incongruent  ·  The raw facts check out (calib corpus 100% stamped a4eNbk…, anchor fit strings say kkHGAC…, suite asserts both at test-judge-calibration.mjs:146/267), but the finding misreads a deliberate, documented, machine-verified design as a stale label. kkHGAC… is exactly digest(PARAMS_A5 minus lifetimeScheme) — verified by probe — i.e. the J7 Tier-2-aggregation-only change that touched no judged-bits input (t1Nodes/t1MultiPv/hashMb/scoreEquivCp/wasm hash unchanged, params.ts:17-39). anchors.ts:20-24 explicitly declares the fit-time digest a frozen LITERAL "so a later params change can never silently re-tag this fitted artifact", and test-judge-calibration.mjs:259-267 documents the corpus-stamp migration to the current digest, with an engine receipt (lines 520-534) re-judging a stored pre-J7 game and proving a byte-identical Tier1Record under the new digest. So the anchor label records fit-time lineage BY DESIGN (corpus stamps were migrated, not the corpus regenerated), the provenance chain is fully reproducible (the suite recomputes every anchor integer from the committed corpus, integer-exact), and the finding's failure scenario — "correcting" the fit string invalidates historical verdicts — is the tamper-evidence property of verdict-pinned anchor digests (tier2.ts:490-493, 915, 972) working as intended; the proposed fix would be actively harmful. No A5 property is threatened: verdict bits, pure-fold purity, Tier-2-only conviction, commit-reveal, and no-node-authority are all untouched, and the corpus is a dev-time calibration artifact, not owner-signed data (no A4-04 class). Not in the documented-accepted (a)-(g) list, so knownAccepted=false; relevant=false because there is no defect to fix beyond, at most, a cosmetic clarifying phrase in a fit string whose rationale is already documented in-code.

# Chess School — autonomous build log (LIVE STATE)

> Resume file for the no-stop autonomous build. READ THIS FIRST after any context reset, then continue.
> Authoritative spec: docs/SCHOOL-SPEC.md. Curriculum (40 ch): docs/school-curriculum.md.
> Mandate: build all 40 chapters; ch1–3 = 3 agents each (reference standard), then 5 at a time 1 agent/ch;
> iterate to PERFECT; re-strategize if quality dips; DO NOT STOP until the user says they're back.
> Loop-alive rule: every turn ends with a running background workflow OR a ScheduleWakeup.

## Phases
- [x] P0 Schema: expand types for chapter→lessons→segments(+puzzle/model)+chapter test. (done)
- [x] P1 Platform: main IPC (lessons, warm-up/cool-down puzzle pulls, test scoring/record) + renderer
      (chapter overview/lesson nav + extended lesson player + test UI), chess.com look. Verify app runs.
- [~] P2 Author ch1–3 (LAUNCHING: 3 agents/ch, author→verify→polish) (3 agents each, opus-max, validate+playtest) → iterate to perfect.
- [ ] P3 Verify ch1–3 perfect (cross-check vs curriculum + correctness + playthrough).
- [ ] P4+ Batches of 5 (ch4–8, 9–13, 14–18, 19–23, 24–28, 29–33, 34–38, 39–40), 1 agent/ch, iterate.

## Schema contract
Location: src/shared/types.ts (School* types). Additive: SchoolChapter gains optional `lessons` + `test`;
legacy `segments` still renders (Knight Forks demo) until replaced. New content authors against `lessons`/`test`.

## Chapter status (40)
Legend: TODO | DRAFTED | VERIFIED | PERFECT
- ch1 Meet the Board & Pieces — VERIFIED (programmatic; visual pending)
- ch2 Special Moves & the Two Goals — VERIFIED (programmatic; visual pending)
- ch3 Check/Mate/Stalemate + M1 — VERIFIED (programmatic; visual pending)
- ch1-8 — AUTHORED + LEGALITY-CLEAN + PEDAGOGICALLY AUDITED (60+ fixes applied via the killed audit run); visual UI pass still pending
- ch9–40 — TODO (see docs/school-curriculum.md)

## Decisions / standards
- Coach = Viktor (exacting). Elo internal-only (never shown). chess.com Lessons look.
- Puzzle segments pull from resources/data/puzzles.sqlite by theme+rating window (reuse puzzles IPC).
- Chapter test: 10–15 Qs, ≥70%, 2 attempts, answers hidden on fail, fail-both ⇒ retake; 2–4 MC + rest board
  (play opening out / judge correct-or-blunder).
- All positions chessops-validated; hooks before early returns (prior #300).

## Running notes (append; newest last)
- 2026-06-27: Mandate received. Built UI to chess.com grade; fixed black-title + hid Elo. Curriculum
  finalized at 40 chapters (logarithmic). Starting P0 (schema) → P1 (platform).

- 2026-06-27: P0+P1 done — schema + platform (ChapterOverview/LessonPlayer/ChapterTest/segments) typecheck+build clean. Launching P2 authoring ch1-3.

- 2026-06-27: ch1-3 authored by 3 opus-max agents each (author/verify/polish), allValid. My independent check: typecheck+build clean; 145 FENs + 75 moves legal. JUDGE CONVENTION: test 'judge' questions store fen = position AFTER the move; lastMoveUci = the move (renderer HIGHLIGHTS it, does not replay) — a naive "is lastMoveUci legal in fen" check is a FALSE POSITIVE; don't flag it. SCHEMA EXTENDED by agents: PuzzleQuery.boards (AuthoredBoard[]) — authored capture positions for the foundation where DB one-move pools are mate-dominated; PuzzleSegment serves them. HARD WALL: user's Mac screen LOCKED (they left) -> computer-use screenshot/click blocked -> VISUAL verification of new lesson/test UI PENDING; retry screenshot when unlocked, then verify ChapterOverview/LessonPlayer/ChapterTest render + fix any platform UI bugs (one-time). Per user mandate, NOT stopping for this. Proceeding to batch 2 (ch4-8). ch1-3 = reference exemplars for later batches.

- 2026-06-27 (user back): Removed knight-forks demo. Built scripts/validate-chapter.mjs (SHARED validator — all agents reuse it; saves tokens, standard correctness) + docs/AUTHOR-AGENT.md (agent playbook: handoff @~150k, reuse validator, schema gotchas). The shared validator caught 2 real bugs my way (ch02 q8, ch05 q3 — false at first due to my own seq-vs-alternatives bug, then fixed; chapters were correct). ch1-5 + ch8 ALL VALIDATE CLEAN. ch6, ch7 still authoring in batch-2 (one of the two failed per user — re-author with new approach once batch settles). New agent model = opus/max (no per-agent "fast"). PLATFORM TODO before opening chapters (ch11+): ChapterTest 'play' renderer accepts only ONE ply; "play the opening OUT" needs multi-ply support.

- 2026-06-27 (CORRECTION): I had wrongly treated "validation" as legality-only and let the 150k token-discipline excuse cutting rigor — WRONG. Validation = TWO GATES: (1) shared validator legality CLEAN, (2) UNBIASED pedagogical playthrough by a FRESH reviewer (helpful / not redundant / sticks / fair test), iterate revise→re-audit until it genuinely passes. 150k = stay sharp + hand off, NEVER do less. AUTHOR-AGENT.md updated; authors now told up front their work WILL be audited. Launched workflow: author ch6+ch7 + pedagogically audit-and-revise ALL of ch1-8 (opus/max, up to 3 audit/revise rounds each). ch1-5/ch8 are legality-clean but NOT yet pedagogically passed — this run fixes that.

- 2026-06-27 (cost reckoning): the audit/revise run (killed) cost ~8M tokens but it audited+revised ALL 8 chapters (~37 lessons, 60+ fixes; tasks #29-93), not 7 lessons. All 8 now validate CLEAN + are pedagogically audited. ROOT CAUSE of cost: opus MAX effort everywhere + every agent re-reading the ~50KB exemplars/curriculum/spec + 3-round loops. LEAN STANDARD GOING FORWARD (ch9-40): opus **high** (not max); agents read AUTHOR-AGENT.md + types.ts + their curriculum slice ONLY (NOT full exemplar JSONs); ONE audit + ONE targeted revise per chapter (I gate, re-run only failures). Target ~200-400k tokens/chapter (≈3-5x cheaper). PAUSED to confirm scale/budget with user before spending ~6-13M more on ch9-40.

- 2026-06-28: Rebuilt+reinstalled app (was STALE: showed 4 ch + knight-forks). VISUAL VERIFICATION DONE & PASSED: index shows 8 named chapters (no knight-forks), ChapterOverview (lesson list + kind chips + Chapter Test entry + progress), LessonPlayer (large board + teaching arrows + Viktor instruction card + Next) all render chess.com-grade. Platform is GOOD. User chose: go lean through ALL ch9-40, don't stop. LEAN standard: opus HIGH effort; agents read AUTHOR-AGENT.md+types.ts+curriculum slice ONLY (not full exemplar JSONs); 1 audit + 1 revise/chapter; ~200-300k tokens/chapter. Opening chapters (ch11 Italian, ch12 London, ch15 Vienna, etc.): teach the line via 'model' segments; TEST with single-ply 'play' + 'judge' (renderer plays one ply — no forced multi-ply). Chaining batches of 5: ch9-13 → 14-18 → ... → 40. Launching ch9-13.

- 2026-06-28: Batch3 (ch9-13) lean done, cost ~1.36M (≈270k/ch — lean target hit, ~4x cheaper than before). Audit caught REAL chess-soundness bugs (this is the pedagogical gate working): ch11 Italian PASSED (Legal's Trap/Fried Liver/copycat all re-verified sound). ch9 (false pin: d7 blocks diagonal), ch10 (fake double-check + 1-of-6 mates accepted), ch12 (UNSOUND Greek Gift sac — teaches wrong thing), ch13 (test = memory quiz of drilled boards) all REVISE'd once but NOT re-audited. Running confirm-loop (re-audit→revise→re-audit) on ch9/10/12/13. Loop fix for batch4+: author→audit→revise→RE-AUDIT until pass (max 3 rounds).

- 2026-06-28: Confirm-loop done. PASS: ch9 (pins/skewers real, verified), ch11 (Italian), ch12 (London — Greek Gift now SOUND + fresh test boards), ch13 (mating patterns — unique mates, transfer test). ch10 STILL FAILS: L4 "Build & Fire the Battery" + test Q8 chess-unsound (target rook undefended → immediate capture is strictly better than the taught "load" move; "can't save it" claim false). 3 revise rounds didn't fix — needs L4 re-keyed to positions where the immediate grab is NOT already winning AND the target can't flee (defended/pinned/tied), or replace with a cleaner discovered-double sub-theme. Launching batch4: ch14-18 (full author→audit→revise→re-audit loop) + targeted ch10-L4 fix, parallel.

- 2026-06-28: SPEED FIX. Batch4 audits crawled ~2h because I told auditors to engine-check EVERY position. All 6 files (ch10,14-18) are WRITTEN + legality-CLEAN; ~3 audit agents still finishing (can't TaskStop the workflow; force-kill risks concurrent-write corruption, so letting it finish). NEW FAST-AUDIT STANDARD for ch19+ (and re-confirms): auditor REASONS about soundness directly (opus knows chess) and engine-spot-checks ONLY the load-bearing tactic of each lesson + every test question — NOT every warm-up/cool-down position. Cap 2 audit rounds (not 3). Keeps the bug-catching power (it found the unsound Greek Gift / false pin via reasoning, not exhaustive engine runs) while being ~3-5x faster. Apply to ch19-23, 24-28, 29-33, 34-38, 39-40 + re-confirm ch7/ch8/ch10/ch14-18 if batch4 verdicts aren't all pass.

- 2026-06-28 (user): SOUNDNESS-CHECK ORDER FIXED. Do NOT brute-force Stockfish. Cross-check content against the ESTABLISHED THEORY/CANON for the subject FIRST (opening mainlines/named traps, standard tactics, textbook endgames — anchor on chess knowledge; WebSearch/WebFetch to confirm a specific line/trap/eval if unsure). Use the bundled engine ONLY to settle a genuine remaining doubt about ONE position — never to scan every position (it's the slowest thing). This replaces the prior "engine-check the load-bearing tactic" wording. AUTHOR-AGENT.md updated. Applies to ch19+ + all re-audits.

- 2026-06-28: NUMBERING BUG FOUND+FIXED. I'd authored ch13-18 from the redistribution workflow's structured array, which DIFFERED from the authoritative table in docs/school-curriculum.md. Re-slotted (content was fine, slots wrong): Scandinavian→ch13, Vienna→ch14, Mating Patterns→ch15, K&P Endgames→ch19, Sacrifice/Greek Gift→ch23. RULE: ALWAYS take chapter number+name from the docs/school-curriculum.md table (lines ~165-224), never memory/structured return. Authored & clean now: ch1-16, ch19, ch23. GAPS to author: ch17 Scotch, ch18 Caro-Kann, ch20 French, ch21 Pawn Structure, ch22 QGD, then ch24 Ruy, ch25 Sicilian-Found, ch26-40. batch4 (ch10,14,15,19,23 + ch16) finished + legality-clean (accepting; were audited). Launching ch17,18,20,21,22 with FAST theory-cross-check audit.

- 2026-06-28: ALL 40 CHAPTERS COMPLETE + legality-CLEAN + typecheck CLEAN + app rebuilt/reinstalled (40 bundled). Anti-stall incremental-write fix made the author-only run finish 16 ch in ~40min @ ~138k/ch (no stalls). ch34 broken-stub re-authored. STATUS: complete & shippable as content. Independent pedagogical AUDIT done for ch1-9,11-23; remaining quality step = independent audit of ch24-40 + re-confirm ch10 L4. Awaiting user decision on that final audit pass.

- 2026-06-30: FINAL AUDIT COMPLETE — ALL 40 CHAPTERS PASS. Ran the independent pedagogical+soundness audit across ch24-40 in 3 targeted fix rounds. Round 3 cleared ch26/27/29/34/36/38. The last two "failures" (ch33, ch35) were verified to be AUDITOR HALLUCINATIONS, not real defects:
  - ch33 boss `6k1/5pp1/7p/3PQ3/8/q7/5PPP/6K1 w`: auditor claimed "boss hangs Black's queen." FALSE. Engine depth-34 MultiPV: top 3 moves are all luft (g3/h3/h4) at +1.48 — a normal clean-extra-pawn conversion, exactly what the intro teaches ("queen sits safe on a3 … make luft first"). No queen-win exists (White's only checks Qe8+/Qb8+ don't hit a3). NO CHANGE.
  - ch35 L6 + Q11/Q12 zugzwang: auditor claimed the moves labeled "draws" actually win. BACKWARDS. Tablebase-depth (100-131) confirmed every label correct: Kf6 wins (mate 15), Kd4/Ke4 draw (cp 0), Kg5 draws (cp 0), Kg6/Ke7 win. Applied 2 minor real wording fixes only: Q11 explain wrongly said g8 "defends" e6 (it's undefended) → fixed; muddled "Kf6 Kd7 Ke6" notation → tidied. Validator still CLEAN.
  - LESSON: LLM auditors hallucinate engine evals — they transcribe a FEN wrong or flip the side to move and "prove" a fake tactic. Always re-verify a flagged soundness defect against the EXACT chapter FEN at real depth before editing. Cross-checking the auditor's claims here PREVENTED corrupting two sound chapters.
  - Deleted all 8 *.fix.txt scratch files. `node scripts/validate-chapter.mjs --all` = ALL 40 CLEAN. Rebuilding + reinstalling app now. SCHOOL CONTENT: DONE — 40/40 chapters legality-clean AND soundness-audited.

- 2026-06-30: PLACEMENT → INTERNAL-ELO → NAME-BASED UNLOCK built (the last unbuilt SCHOOL-SPEC pillar, §1/§2.2a). End-to-end vertical slice, typecheck+build+boot clean, v4 migration verified live (user_version=4, school_placement+placement_game tables, seed row placed=0). Independent adversarial review = SHIP (8/8 checks OK: no Elo leak to renderer, React #300 hooks clean, migration additive/atomic, engine uciElo≥1320 floor respected, zod/preload/type aligned, unlock = clean monotonic prefix, double-score guarded). Pieces:
  - DATA: each of 40 chapter JSONs stamped eloFloor = its internal band's low end (from docs/school-curriculum.md), monotonic 0→1950. SchoolChapter.eloFloor; SchoolChapterMeta.locked/lockReason (NAME-based, never an Elo).
  - DB v4 (database.ts): school_placement (single-row id=1; estimated_elo OVERWRITTEN not Math.max'd so it can move down = mis-placement) + placement_game. Kept separate from Glicko `rating`.
  - placement.repo.ts: getPlacementState, recordPlacementGame (inverse-variance blend of estimateElo bands over games), bumpPlacementFloor (raise-only, on test pass), resetPlacement, PLACEMENT_ENGINE_ELO=1500.
  - mastery.repo getMastery now READS BACK lesson_progress (was write-only → done-badges reset every mount) → returns `lessons`; ChapterPlayer seeds doneLessonIds from it.
  - school.repo chapterMetas computes locked = !placed || eloFloor>estElo; surfaces only the boolean+reason.
  - IPC/preload: school:placementState/recordPlacementGame/resetPlacement/placementConfig; recordTest bumps floor on pass.
  - Renderer: PlacementFlow.tsx (one game as White vs 1500 engine → review.run for accuracy → recordPlacementGame; result screen 100% qualitative, NO Elo shown; retries review once on the single-flight race). SchoolView gates on placementState (PlacementFlow when !placed), locked ChapterCards dashed "unlocks as you climb"; stat shows Unlocked count.
  - DESIGN DEFAULTS chosen under "you choose what's best" (all adjustable): 1 placement game as White; test-pass raises (never lowers) estimate; review-fail fallback ≈765 internal Elo. STILL P1 (deferred): backend test-scoring authority + fail-both relock; multi-ply "play opening out" test; chess.com 1:1 puzzle-rating rework; proactive in-game coaching; upload mac Stockfish binary to datasets-v1.

- 2026-06-30 (fleet build): PUZZLES FINISHED + 4 SCHOOL IDEAS BUILT, both shipped (typecheck+build+boot CLEAN, migrations verified live to user_version=6). Done with two background agent fleets + a final single rebuild/reinstall.
  - PUZZLES (DB v5 — puzzle_attempt.theme/mode, puzzle_rush_run, daily_result): 4 modes — Train (adaptive classic), Custom/Themed (themes+difficulty+length → focused set + accuracy-ring summary; mode='custom' doesn't move Glicko), Rush/Storm (rush3/rush5/storm/survival; prefetched/refilling queue; climbing rating band; lives/clock HUD; own high-score, mode='rush' does NOT move Glicko), Daily (deterministic FNV-1a daily + local streak + cross-mode stats + paginated history). PuzzlesView is now a mode router; Train preserved verbatim. Files: src/main/db/{puzzles.repo batchPuzzles, rush.repo, daily.repo}, src/main/ipc/{puzzles,puzzles.rush,puzzles.daily}.ipc, renderer features/puzzles/modes/{CustomMode,RushMode,DailyMode}.
  - SCHOOL (DB v6 — concept_srs, school_day): (1) recordTest now SERVER-AUTHORITATIVE (recompute passed from chapter.test.passThreshold; clamp attempts=MAX_ATTEMPTS=2; true-2nd-fail → resetChapterForRetake (wipe lesson_progress, zero chapter_progress + chapter_test attempts/passed, KEEP best_pct); returns TestRecordResult; placement bump gated on server verdict); TestPlayQuestion gains optional multi-ply `line` (back-compat with single-ply solutionUci). (2) recommendNextChapter() — weakness-ranked unlocked-unpassed chapter, name-based reason (never Elo), in src/main/school/recommend.ts. (3) SM-2-lite spaced repetition — src/main/rating/fsrs.ts (pure scheduler: 10min→1day→graduate 3day, EF [1.3,3.0], cap 365d) + src/main/school/srs.repo.ts (getDueConcepts/reviewConcept, self-seeding). (4) Daily lesson + LOCAL-day streak — src/main/school/daily.repo.ts + src/main/util/day.ts (local, not UTC); a day counts when lesson_done OR review_done. Foundations: school.repo conceptToChapter() reverse index; mastery.repo appendProgressEvent() (writes the previously-dead progress_event) hooked into recordConcept/recordLesson/recordTest. Renderer: TodayCard + RecommendedNextCard on the School home; ChapterTest renders the server verdict + multi-ply loop.
  - DECIDED DEFAULTS (owner said "go"): local-day streaks; SM-2-lite (not full FSRS); a study action = finishing a lesson OR an SRS review; one "Today" surface (reviews-due folded in). All adjustable.
  - REMAINING: chess.com 1:1 rating rework (design-forked; Glicko untouched); proactive in-game Viktor coaching; mac Stockfish upload to datasets-v1 (needs owner GitHub login); conceptId-on-test-questions authoring would sharpen weakness targeting from tests (per-concept, not just per-chapter). Runtime click-through of the new modes/surfaces is the only unburned verification.
